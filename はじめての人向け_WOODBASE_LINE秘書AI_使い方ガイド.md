# はじめての人向け WOODBASE LINE秘書AI 使い方ガイド

作成日: 2026-07-07

この資料は、WOODBASE LINE秘書AIを初めて使う人が「結局これは何で、何をどう使えばいいのか」を迷わないための説明書です。  
細かい設計やコードの話ではなく、実際の使い方を中心にまとめています。

---

## 1. 結論：この仕組みで何ができるのか

WOODBASE LINE秘書AIは、LINEの会話を入口にして、案件・タスク・予定・進捗・見積・ファイルを自動で整理するための仕組みです。

普段どおりLINEでやり取りしながら、必要なときだけBotに声をかけると、以下のことをしてくれます。

| できること | 何が便利か |
|---|---|
| 案件を登録する | 新しい仕事が出たとき、案件台帳・進捗管理・Driveフォルダを作る |
| 進捗を更新する | 「見積出した」「着工した」「来週フォロー」などを進捗表へ反映する |
| タスクを作る | 「田中さんに確認」「4/25までに見積」などをタスク化する |
| 予定を登録する | 現調・打合せ・納品などをスケジュールとGoogleカレンダーへ入れる |
| 進捗表に質問する | 「未完了の店舗ある？」「図面チェックまだ？」などをLINEで聞ける |
| 見積書を作る | 工程管理表から月別の見積書シートを作る |
| ファイルを保存する | LINEに送られた画像・PDF・動画をDriveの案件フォルダへ保存する |
| 案件サマリーを作る | 会話履歴やタスクをまとめてGoogle Docs化する |
| AppSheet/アプリから操作する | LINE以外に、ボード・承認・ジョブ実行用の画面を作れるようにしている |

一言でいうと、**LINEで話した内容を、後から探せる案件管理データに変える秘書**です。

---

## 2. まず覚えることは3つだけ

### 2-1. DMならそのまま話してOK

Botとの1対1トークでは、普通に送れば反応します。

例:

```text
雨のち晴れクリニックの見積を4/25までに深田さんお願いします
```

### 2-2. グループではBotを呼ぶ

LINEグループでは、普段の会話にいちいちBotが反応しないようにしています。  
Botに処理してほしいときは、文頭に呼び出し語を付けます。

使える呼び出し語:

```text
@WBG
/WBG
/wbg
秘書:
秘書：
```

例:

```text
@WBG 雨のち晴れクリニック、見積提出しました。4/25にフォローします
```

### 2-3. 案件名・担当・日付を入れると精度が上がる

Botは自然文を読めますが、次の3つが入っているほど正確です。

| 入れるとよい情報 | 例 |
|---|---|
| 案件名 | 雨のち晴れクリニック、理容プラージュ 蒲田 |
| 担当者 | 深田さん、田中さん、経理 |
| 日付 | 4/25まで、来週月曜、7/3 14時 |

---

## 3. 使う場所の違い

### 3-1. LINE

一番よく使う入口です。  
現場・営業・管理者が、案件の進捗、タスク、予定、質問を送ります。

向いていること:

- その場で進捗を報告する
- 担当者へタスクを振る
- 現調や打合せ予定を入れる
- 未完了や進捗状況を聞く
- 見積書作成を依頼する
- 案件サマリーを作る

### 3-2. Googleスプレッドシート

Botが整理したデータの保存先です。  
管理者や経理が、台帳・承認・設定を確認します。

主なシート:

| シート名 | 役割 |
|---|---|
| projects | 案件の中心台帳。案件ID、案件名、フェーズ、次回アクション、金額、請求、入金など |
| プロジェクト管理 | 案件名の辞書。略称、正式名称、LINEグループIDなど |
| 案件グループ | 1案件に複数のLINEグループをぶら下げる台帳 |
| タスク管理 | LINEから作られたタスク |
| スケジュール管理 | LINEから作られた予定 |
| project_pending_updates | 金額・請求・入金など、承認待ちの更新 |
| project_logs | 進捗変更の履歴 |
| pmジョブ | AppSheetなど外部アプリからGASへ処理を依頼するジョブ |
| 見積ジョブ | アプリやシートから見積作成を依頼するジョブ |
| 見積連携設定 | 工程管理表と見積書テンプレートの対応表 |

### 3-3. Google Drive

案件フォルダ、LINE添付ファイル、Docsサマリー、見積書などの保存先です。

新規案件を作ると、案件用のDriveフォルダを作れる設計になっています。  
LINEに画像・PDF・動画が投稿された場合も、案件に紐付いていれば案件フォルダへ保存します。

### 3-4. Googleカレンダー

現調、打合せ、納品など日時がある予定を登録します。  
LINEから予定を送ると、スケジュール管理シートとGoogleカレンダーの両方に入ります。

### 3-5. Webダッシュボード

`app_dashboard.js` と `dashboard.html` で、案件の進捗ボードをGAS Webアプリとして表示できます。

できること:

- 案件をフェーズ別に見る
- 案件の詳細を見る
- フェーズを変更する
- 案件をアーカイブする
- 請求・入金の申請を出す
- 新規案件を追加する

アクセス保護として `DASHBOARD_TOKEN` を使い、URL末尾に `?key=...` を付ける設計です。

### 3-6. AppSheet

AppSheetは、スプレッドシートを直接触らずに操作するためのアプリ化方針です。  
現在の設計では、進捗ボード・承認・見積ルール管理をAppSheetから使う想定です。

重要な考え方:

- フェーズや次回アクションはアプリから直接更新してよい
- 売上・原価・請求・入金は勝手に確定せず、承認待ちに入れる
- 承認・却下・Drive作成・カレンダー同期などは、`pmジョブ` や `見積ジョブ` に行を追加し、GASの定期実行が処理する

---

## 4. LINEで使える主なコマンドと例

厳密なコマンドだけでなく、自然な文章でも動きます。  
ただし、最初のうちは下の型に寄せると失敗しにくいです。

### 4-1. 新しい案件を作る

使う場面:

- 新しい案件が始まった
- まだ案件台帳にない仕事を登録したい

入力例:

```text
@WBG 新規案件 案件名：山田ビル客室改装 クライアント：ABC運営 担当：深田
```

最低限これでもOK:

```text
@WBG 新規案件 山田ビル客室改装
```

Botがやること:

- 新しい案件IDを採番する
- `projects` に案件を追加する
- 必要に応じてDriveフォルダを作る
- LINEグループIDを案件に紐付ける

注意:

- 同じ名前の案件がすでにある場合は、既存案件として扱われます。
- 似た案件が複数ある場合は、Botが候補を出して選ばせます。

---

### 4-2. 進捗を更新する

使う場面:

- 見積を出した
- 受注した
- 着工した
- 修正対応中
- 次にやることが決まった

入力例:

```text
@WBG 案件更新 雨のち晴れクリニック、見積提出しました。4/25にフォローします
```

```text
@WBG 理容プラージュ 蒲田、着工しました。来週中間検査です
```

```text
@WBG Amuyo渋谷店、客先確認待ち。深田さんが来週修正対応します
```

Botがやること:

- 案件を特定する
- フェーズやステータスを読み取る
- 次回アクションと期限を抽出する
- `projects` を更新する
- `project_logs` に履歴を残す
- 次回アクションに期限があれば、フォロータスクを作る
- 必要に応じてカレンダー同期する

金額が含まれる場合:

```text
@WBG 山田ビル客室改装、見積提出。売上予定500万です
```

売上・原価・請求・入金などの金額系は、事故防止のため自動確定しません。  
経理・管理者の承認待ちに入ります。

---

### 4-3. 請求・入金を報告する

使う場面:

- 請求予定が決まった
- 請求済みにしたい
- 入金予定が決まった
- 入金確認済みにしたい

入力例:

```text
@WBG 請求更新 雨のち晴れクリニック、7/1に請求予定です
```

```text
@WBG 入金更新 山田ビル客室改装、7/10入金予定です
```

Botがやること:

- いったん `project_pending_updates` に承認待ちとして登録する
- 経理・管理者へ確認通知を送る
- 承認されたら `projects` に反映する

ポイント:

- 請求・入金は「報告しただけ」では確定しません。
- 経理が承認して初めて正式反映されます。

---

### 4-4. タスクを作る

使う場面:

- 誰かに作業をお願いしたい
- 期限付きのTODOを残したい

入力例:

```text
@WBG 深田さん、雨のち晴れクリニックの見積もりを4/25までにお願いします
```

```text
@WBG 田中さんに、理容プラージュ 蒲田の図面確認を今週中に依頼
```

Botがやること:

- 担当者を読み取る
- 内容をタスク化する
- 期限を読み取る
- `タスク管理` に登録する
- 担当者がメンバー管理に登録されていればDM通知する

緊急ワード:

```text
至急
急ぎ
今日中
明日まで
ASAP
最短
```

これらが入っていると、緊急度が高いタスクとして扱われます。

---

### 4-5. タスクを完了にする

使う場面:

- 対応が終わった
- 指示された作業を完了した

入力例:

```text
@WBG 完了しました
```

```text
@WBG 雨のち晴れクリニックの見積対応、終わりました
```

Botがやること:

- 対象タスクを推定する
- タスクのステータスを完了にする
- 完了タスクとしてアーカイブ対象にする

注意:

- 似たタスクが複数あると、Botがうまく特定できない場合があります。
- その場合は、案件名や作業内容を一緒に書いてください。

---

### 4-6. 予定を登録する

使う場面:

- 現調
- 打合せ
- 納品
- 検査
- 引き渡し

入力例:

```text
@WBG 予定追加 雨のち晴れクリニック 7/3 14時 現地打合せ
```

```text
@WBG 山田ビル客室改装、7/10 10時から現調です。参加者は深田さん
```

Botがやること:

- 日付・時刻・予定名・参加者を読み取る
- `スケジュール管理` に登録する
- Googleカレンダーにも登録する
- 同じ予定がすでにあれば重複登録を避ける

足りない情報がある場合:

- 日付がない
- 時刻がない
- 場所や参加者が曖昧

この場合、Botが補足質問を返すことがあります。

---

### 4-7. 残タスクや予定を聞く

使う場面:

- 自分の残タスクを確認したい
- 案件ごとの未完了タスクを見たい
- 今週の予定を確認したい

入力例:

```text
@WBG 残タスクは？
```

```text
@WBG 雨のち晴れクリニックの残タスクは？
```

```text
@WBG 今週の予定は？
```

Botがやること:

- タスク管理やスケジュール管理を検索する
- 該当するものをLINEに返す

---

### 4-8. 進捗管理表に質問する

使う場面:

- 進捗管理表を開かずに未完了を知りたい
- 図面チェックや発注など、どの店舗が止まっているか知りたい

入力例:

```text
@WBG 図面チェックできてない店舗ある？
```

```text
@WBG 5月の図面チェックまだのところ教えて
```

```text
@WBG 確認中の店舗だけ教えて
```

```text
@WBG 6月竣工で未完了ある？
```

Botがやること:

- 外部の進捗管理表を読む
- ヘッダー行を自動判定する
- 「完了」「未完了」「確認中」などを判定する
- 月、店舗、担当者、状態で絞り込む
- 最大30件程度にまとめて返す

注意:

- `PROGRESS_SPREADSHEET_ID` が設定されている必要があります。
- 進捗管理表がGAS実行アカウントに共有されている必要があります。

---

### 4-9. 見積書を作る

使う場面:

- 工程管理表から、対象月の見積書を自動作成したい
- 店舗や会社ごとの見積シートを作りたい

入力例:

```text
@WBG 東邦家具 5月分の見積書作って
```

```text
@WBG toho 2026年6月の見積作成
```

```text
@WBG 見積設定一覧
```

Botがやること:

- `見積連携設定` から対象設定を探す
- 工程管理表を読む
- 対象月の行を集計する
- 見積書テンプレートをコピーする
- 月別シートを作る
- 金額、消費税、合計を反映する
- 自動処理できない行は `見積要確認` に記録する

注意:

- 同じ月の見積シートがすでにある場合、上書きせず処理を止めます。
- 見積対象を増やすには、`見積連携設定` に1行追加します。
- 工程管理表と見積書は `woodbasegroup@gmail.com` に編集権限で共有されている必要があります。

---

### 4-10. 案件サマリーを作る

使う場面:

- 案件の状況をまとめたい
- 会話履歴、タスク、予定をDocsに残したい
- 引き継ぎ用の資料を作りたい

入力例:

```text
@WBG 雨のち晴れクリニック まとめて
```

```text
@WBG 雨のち晴れクリニック Docs更新
```

```text
@WBG 案件サマリー作成
```

Botがやること:

- 対象案件を特定する
- メッセージログ、タスク、予定を集める
- Geminiで要約する
- Google Docsへ保存または更新する

---

### 4-11. LINEに送られたファイルを保存する

使う場面:

- 現場写真
- 図面PDF
- 動画
- 資料ファイル

使い方:

案件に紐付いたLINEグループに、画像・PDF・動画を送ります。

Botがやること:

- LINEからファイルを取得する
- 案件フォルダへ保存する
- 案件が特定できない場合は未分類フォルダへ保存する

---

## 5. 案件名が曖昧なときの動き

Botは、AIだけで勝手に案件を決めきらない設計です。  
似た案件が複数あるときは、候補を出して選ばせます。

例:

```text
@WBG プラージュ、見積提出しました
```

プラージュ案件が複数ある場合、Botは候補を返します。

- 理容プラージュ 蒲田
- 理容プラージュ 旭川永山
- 美容プラージュ 川西
- 新規案件として登録

正しい候補をタップすると、その案件に反映されます。

---

## 6. 承認が必要なもの

次の情報は、Botやアプリが勝手に確定しません。

| 項目 | 理由 |
|---|---|
| 売上 | 経理・管理者確認が必要 |
| 原価 | 経理・管理者確認が必要 |
| 請求予定日 | 請求漏れ防止のため確認 |
| 請求済み | 誤登録防止 |
| 入金予定日 | 経理確認が必要 |
| 入金確認済み | 誤登録防止 |

LINEやアプリから申請すると、`project_pending_updates` に入ります。  
承認されると `projects` に反映されます。

---

## 7. 自動で毎日・毎週動くもの

GASトリガーにより、定期的に自動処理が走ります。

| タイミング | 処理 |
|---|---|
| 毎分 | `runAppJobs()` が `pmジョブ` と `見積ジョブ` を処理 |
| 毎日朝 | 期限リマインド、連絡漏れチェック |
| 毎日朝 | 進行中案件のDocsサマリー更新 |
| 毎週 | 週次レポート |
| 定期 | 完了タスクのアーカイブ |

自動通知の例:

- 期日が近いタスク
- 期日を過ぎたタスク
- 見積提出後に更新が止まっている案件
- 請求予定日を過ぎた案件
- 入金予定日を過ぎた案件

---

## 8. AppSheet・Webアプリでできること

LINEだけでなく、画面から操作するための設計も追加されています。

### 8-1. 進捗管理ボード

目的:

スプレッドシートを直接見ずに、案件をフェーズ別に見て更新する。

できること:

- 案件をフェーズ別ボードで見る
- 担当者や期限を見る
- フェーズを変更する
- 次回アクションを確認する
- 案件詳細を見る

### 8-2. 承認画面

目的:

売上・原価・請求・入金を、経理または管理者が確認して確定する。

流れ:

1. LINEまたはアプリから金額系の申請が入る
2. `project_pending_updates` に承認待ちとして保存される
3. 経理・管理者が承認または却下する
4. 承認なら `projects` に反映される
5. 履歴が `project_logs` に残る

### 8-3. ジョブ方式

AppSheetから直接重い処理を実行せず、シートに「依頼行」を追加します。  
GASが毎分その依頼を処理します。

使うシート:

| シート | 役割 |
|---|---|
| pmジョブ | 承認、却下、カレンダー同期、Driveフォルダ作成など |
| 見積ジョブ | 見積書作成 |
| アプリ権限 | アプリ利用者のメール、役割、LINEユーザーID |

メリット:

- AppSheet側を軽くできる
- GASの既存処理を再利用できる
- 処理結果やエラーをシートに残せる
- 完了通知をLINEへ送れる

---

## 9. 案件IDと案件グループの考え方

2026-07-07時点で、案件管理は「案件ID」を中心に整理する方針になっています。

### 9-1. 案件ID

案件IDは、1つの案件を間違えずに追跡するための番号です。

例:

```text
2026-001
2026-003
2026-050
```

案件名は後から変わることがありますが、案件IDは変えません。

### 9-2. projects と プロジェクト管理

同じ案件IDを使って、2つのシートをつなぎます。

| シート | 役割 |
|---|---|
| projects | 進捗、金額、請求、入金、次回アクションなど |
| プロジェクト管理 | 正式名称、略称、LINEグループID、会社情報など |

案件名を直す場合は、両方のシートで同じ案件IDの行を直します。

### 9-3. 案件グループ

1つの案件に、複数のLINEグループが関係することがあります。

例:

- 施主グループ
- 業者グループ
- 社内グループ
- 行政協議グループ

これを整理するために `案件グループ` シートを使います。

ポイント:

- 進捗管理対象の案件には案件IDを入れる
- タスク専用や社内連絡用のグループは、案件IDを空欄にして台帳として残す
- 複数店舗をまたぐ業者グループは、無理に1案件へ紐付けない

---

## 10. 管理者が最初に設定すること

### 10-1. Script Properties

GASのScript Propertiesに、必要なキーを設定します。

| キー | 役割 |
|---|---|
| LINE_CHANNEL_ACCESS_TOKEN | LINE Botのアクセストークン |
| LINE_CHANNEL_SECRET | LINE Webhook検証用 |
| GEMINI_API_KEY | Gemini APIキー |
| SPREADSHEET_ID | メイン管理スプレッドシート |
| DRIVE_FOLDER_ID | Drive保存先フォルダ |
| CALENDAR_ID | GoogleカレンダーID |
| INTERNAL_GROUP_ID | 社内通知先LINEグループ |
| ACCOUNTING_GROUP_ID | 経理通知先LINEグループ |
| PROGRESS_SPREADSHEET_ID | 外部の進捗管理表 |
| PROGRESS_SHEET_NAME_OVERRIDE | 進捗管理表のシート名 |
| DASHBOARD_TOKEN | Webダッシュボードのアクセスキー |

### 10-2. 初期セットアップで実行する関数

GASエディタから実行します。

| 関数 | 目的 |
|---|---|
| `setup()` | 基本シートを作る |
| `pmSetupSheets()` | projects、project_logs、案件グループなどPM系シートを作る |
| `pmJobsSetupAll()` | pmジョブ、見積ジョブ、アプリ権限を作り、毎分トリガーを設定 |
| `setupEstimateSheets()` | 見積連携設定、見積単価設定、見積要確認を作る |
| `setupProgressSpreadsheet(id, sheetName)` | 外部進捗管理表を設定 |
| `createTriggers()` | 期限通知などの定期トリガーを作る |
| `setupSummaryTrigger()` | Docsサマリー更新トリガーを作る |

### 10-3. 権限共有

次のものは、GAS実行アカウント `woodbasegroup@gmail.com` に編集権限で共有します。

- メイン管理スプレッドシート
- 進捗管理表
- 工程管理表
- 見積書スプレッドシート
- Drive保存先フォルダ
- Googleカレンダー

---

## 11. よくある困りごと

### Botが反応しない

確認すること:

- グループでは `@WBG` や `/WBG` を付けているか
- LINE DevelopersのWebhookが有効か
- GAS WebアプリURLがWebhookに設定されているか
- `LINE_CHANNEL_ACCESS_TOKEN` が正しいか
- `LINE_CHANNEL_SECRET` とWebhook URLの検証情報が合っているか
- GASの実行ログにエラーが出ていないか

### 違う案件に入りそうになる

対処:

- 案件名をもう少し詳しく書く
- Botが候補を出したら正しい案件を選ぶ
- `プロジェクト管理` と `projects` の案件名・案件IDを確認する
- グループと案件の紐付けを確認する

### タスクが登録されない

確認すること:

- 担当者、内容、期限が読み取れる文章になっているか
- 単なるお礼や承知連絡ではないか
- 48時間以内に同じタスクがあり、重複としてスキップされていないか
- 案件候補が曖昧で、まだ選択待ちになっていないか

### 請求・入金が反映されない

確認すること:

- `project_pending_updates` に承認待ちで入っていないか
- 経理・管理者が承認済みにしたか
- `ACCOUNTING_GROUP_ID` または `INTERNAL_GROUP_ID` が設定されているか
- AppSheetやWebアプリの場合、`pmジョブ` が処理済みになっているか

### 見積書が作れない

確認すること:

- `見積連携設定` に対象設定があるか
- `有効` が TRUE になっているか
- 工程管理表ID、シート名、見積書ID、テンプレートシート名が正しいか
- 対象月のデータが工程管理表にあるか
- 同じ月の見積シートが既に存在していないか
- GAS実行アカウントに編集権限があるか

### 進捗管理表の質問に答えない

確認すること:

- `PROGRESS_SPREADSHEET_ID` が設定されているか
- `PROGRESS_SHEET_NAME_OVERRIDE` が実際のシート名と一致しているか
- 進捗管理表が共有されているか
- `diagnoseProgressSheet()` でヘッダー行が検出できるか

---

## 12. 初めて使う人向けのおすすめ入力例

### 案件を作る

```text
@WBG 新規案件 案件名：山田ビル客室改装 クライアント：ABC運営 担当：深田
```

### 進捗を報告する

```text
@WBG 山田ビル客室改装、見積提出しました。7/10にフォローします
```

### タスクを頼む

```text
@WBG 田中さん、山田ビル客室改装の図面確認を7/8までにお願いします
```

### 予定を入れる

```text
@WBG 予定追加 山田ビル客室改装 7/9 14時 現地打合せ
```

### 請求予定を申請する

```text
@WBG 請求更新 山田ビル客室改装、7/31に請求予定です
```

### 入金予定を申請する

```text
@WBG 入金更新 山田ビル客室改装、8/10入金予定です
```

### 残タスクを見る

```text
@WBG 山田ビル客室改装の残タスクは？
```

### 進捗表に聞く

```text
@WBG 5月の図面チェックまだのところ教えて
```

### 見積書を作る

```text
@WBG 東邦家具 5月分の見積書作って
```

### 案件まとめを作る

```text
@WBG 山田ビル客室改装 Docs更新
```

---

## 13. どのファイルに何が入っているか

開発・保守する人向けのざっくり対応表です。

| ファイル | 役割 |
|---|---|
| `コード.js` | 既存のLINE Bot本体、Webhook、基本処理 |
| `pm_core.js` | PM機能の共通定数、シート定義、基本ヘルパー |
| `pm_router.js` | LINEからの案件更新、タスク、予定、承認待ちへの振り分け |
| `pm_parser.js` | 案件更新文の解析 |
| `pm_manager.js` | projects更新、ログ、案件検索 |
| `pm_pending.js` | 承認待ち、承認、却下 |
| `pm_taskbridge.js` | 次回アクションからタスクを作る |
| `pm_calendar.js` | 予定・カレンダー連携 |
| `pm_drive.js` | 案件フォルダ作成 |
| `pm_alert.js` | 放置・請求漏れ・入金漏れなどのアラート |
| `pm_jobs.js` | AppSheet/Webアプリからのジョブ処理 |
| `pm_groups.js` | 案件グループ管理 |
| `pm_organize.js` | 案件名整理・グループ整理系 |
| `pm_backfill.js` | 既存データへの案件IDやグループ情報の補完 |
| `pm_tests.js` | PM系テスト |
| `progress.js` | 進捗管理表への自然文質問 |
| `estimate.js` | 工程管理表から見積書を作る処理 |
| `estimate_pioneer.js` | 見積関連の別系統・補助処理 |
| `summary.js` | 案件サマリー、Google Docs連携 |
| `app_dashboard.js` | GAS Webダッシュボードのサーバー側処理 |
| `dashboard.html` | Webダッシュボード画面 |
| `migrate.js` / `migrate_admin.js` | 移行・管理補助 |
| `reorg_20260707.js` | 2026-07-07時点の整理作業補助 |

---

## 14. 運用ルールのおすすめ

### 普段の現場・営業メンバー

- グループでは文頭に `@WBG` を付ける
- 案件名をできるだけ入れる
- 担当者と期限を入れる
- 金額・請求・入金は「申請になる」と理解しておく
- Botが候補を出したら、正しい案件を選ぶ

### 経理

- `project_pending_updates` または承認画面を見る
- 請求・入金・金額系は承認してから確定する
- 請求漏れ・入金漏れ通知を確認する

### 管理者

- 新しい案件名やグループ紐付けを整える
- `projects` と `プロジェクト管理` の案件IDを揃える
- `案件グループ` を整理する
- 見積対象が増えたら `見積連携設定` を追加する
- AppSheetやWebダッシュボードの権限を管理する

---

## 15. 最短の理解

初めての人は、まずこれだけ覚えれば使い始められます。

1. LINEグループでは `@WBG` を文頭に付ける
2. 案件名、担当者、期限を書く
3. 進捗はそのまま報告する
4. 金額・請求・入金は承認待ちになる
5. わからなければ「残タスクは？」「今週の予定は？」「未完了ある？」と聞く

この仕組みは、完璧な文章を書くためのものではありません。  
日々のLINE連絡を、後から追える形に変えるためのものです。
