# AppSheet設計図｜パイロット2：進捗管理ボード（v1）

作成: 2026-06-30 / 目的: スプシを直接見ずに、案件をフェーズ別ボードで把握・更新・承認する。
前提: データはメインスプシのまま。**金額/請求/入金は承認制を維持**（アプリで直接確定させない）。通知/カレンダー/Drive/帳票はGASに残しアプリから呼ぶ。
依存: フェーズ1（案件IDで二面一体）のステップ1〜4が完了していること（`フェーズ1_整理プラン_案件ID統合.md`）。構築はWorkspace移行後のスプシ上で。

---

## 1. アプリ概要

| 項目 | 内容 |
|---|---|
| アプリ名 | 進捗管理ボード（仮） |
| 利用者 | 全員（閲覧＋進捗更新）／経理（金額承認）／管理者（全権） |
| デバイス | PC＋スマホ |
| データ源 | projects(主) ＋ プロジェクト管理 ＋ project_pending_updates ＋ タスク管理 ＋ 進捗管理表 ＋ project_logs |
| 結合キー | **案件ID**（全テーブル共通／フェーズ1で付与） |

---

## 2. テーブル設計（既存シートを結合）

| テーブル | キー | 役割 | 主な列（抜粋） |
|---|---|---|---|
| `projects` ★主 | 案件ID | 進捗・金額・日付 | 案件名, 現在フェーズ, 各フェーズステータス, 売上/原価/利益, 請求/入金系, 次回アクション(+期限), 担当者, 見積プロファイルID |
| `プロジェクト管理` | 案件ID(追加) | 辞書：会社/住所/グループ | 略称, 正式名称, グループID(施主/業者), 会社名, 住所 |
| `project_pending_updates` | 確認ID | 承認待ち | 案件ID, type, intent, 抽出内容, status, 申請者 |
| `タスク管理` | （行ID） | 案件の派生タスク | タスク名, 担当, 期日, 状態, 関連案件ID |
| `進捗管理表`(外部) | （行ID） | 施工チェックリスト | 店舗名, 施工月, 図面チェック…, 案件ID(追加) |
| `project_logs` | ログID | 変更履歴(読取) | 日時, 案件ID, intent, 変更項目, 更新者 |

**参照(Ref)関係**: プロジェクト管理.案件ID / pending.案件ID / タスク管理.関連案件ID / 進捗管理表.案件ID → すべて `projects.案件ID` を指す。

---

## 3. ビュー設計

| ビュー | 種類 | 内容 |
|---|---|---|
| 案件ボード | Kanban（現在フェーズで列） | カード=案件名/担当/次回アクション/期限。ドラッグでフェーズ移動（→§4で更新＋ログ） |
| 案件一覧 | Table/Deck | フィルタ: フェーズ/担当/クライアント/放置フラグ |
| 案件詳細 | Detail | 進捗(projects)＋辞書(会社/住所/グループ)＋子タスク＋承認待ち＋施工チェック＋履歴＋各リンク(Drive/Docs/Calendar) |
| 進捗更新 | Form | フェーズ/各ステータス/次回アクション(+期限)/担当/備考 のみ編集可 |
| 承認 | Table＋アクション | 経理が 承認/却下（金額・請求・入金） |
| 金額申請 | Form | 売上/原価/請求/入金は**直接編集せず**ここから申請→pendingへ |
| ダッシュボード | Chart/Cards | フェーズ別件数・放置・請求漏れ・入金未確認の件数 |
| タスク | Board/Table | 案件横断のタスク一覧（担当/期日/状態） |
| 施工チェックリスト | Table | 案件詳細から該当店舗の未完了項目 |

---

## 4. 更新ルール（既存の安全装置を維持）

| 操作 | アプリでの扱い | 反映 | ログ |
|---|---|---|---|
| フェーズ/ステータス変更 | ボードのドラッグ or フォーム | projects を直接更新（自動更新OK項目） | project_logs に追記（AppSheet自動化 or GAS） |
| 次回アクション/期限/担当/備考 | フォーム編集 | projects 直接更新 | 同上 |
| **売上/原価/請求/入金/請求済/入金確認** | **直接編集不可**。「金額申請」フォーム→ `pmジョブ` に行追加（種別=`請求申請`/`入金申請`、依頼者=USEREMAIL()）。GASが承認待ち登録＋**経理グループへLINE通知**（承認/却下ボタン付き） | 承認後に確定。**最終更新者=申請者（アプリ権限の表示名）を自動記録** | 申請・承認の両方を記録 |
| 承認/却下 | 「承認」ビューのボタン | pending.status を更新 → GAS が projects へ確定反映（既存 `pmApprovePending` 相当をジョブで実行） | 適用区分=approved |
| カレンダー/Drive/サマリー/見積作成 | 案件詳細のボタン | **GASを呼ぶ**（ジョブ方式）。AppSheetは結果リンクを表示 | — |

**ポイント**:
- 「自動更新OK（フェーズ/次回/担当）」と「確認必須（金額/請求/入金）」の既存方針（設計書§8）を**そのままUIに反映**。アプリが勝手に金額を確定しない。
- ステータスは `PM_PHASES`（pm_core.js）の許可値を Enum 化（表記ゆれ防止）。
- **ログ整合**: アプリ起点の変更も project_logs に残す。実装は (a) AppSheet自動化でログ行追加、または (b) GAS側のジョブで一括記録。**(b)を推奨**（既存 `pmAddLog` を再利用でき、整合が取りやすい）。

---

## 5. アプリ↔GAS 連携（ジョブ方式・パイロット1と共通）

承認確定・カレンダー同期・Drive作成・サマリー・見積作成は、AppSheetが**ジョブ行を追加**→GASの毎分トリガーが処理→結果書戻し、で統一する。

```
[アプリ] ボタン（承認/カレンダー同期/見積作成 等）
   → 該当ジョブテーブルに行追加（種別・案件ID・パラメータ）
        ▼
[GAS] runPmJobs()（新規・毎分）  ← パイロット1の runEstimateJobs() と同じ枠組み
   → 種別で分岐: pmApprovePending相当 / pmSyncCalendar / pmEnsureProjectFolder / 見積発行
   → 既存関数を再利用して実行 → 状態/結果を書戻し
```

- 既存の `pmApprovePending` / `pmSyncCalendar` / `pmEnsureProjectFolder` を**そのまま再利用**（壊さない）。
- 通知（放置/請求漏れ/入金未確認）は従来どおりGASの毎朝トリガー。アプリは結果をダッシュボードで見る。

> ★実装済み(2026-07-07・`pm_jobs.js`): `runPmJobs()` 実装済み。毎分トリガーは `runAppJobs()` に集約（見積ジョブと共通）。セットアップは `pmJobsSetupAll()` 1回。
> - `pmジョブ` シート＝['ジョブID','依頼日時','依頼者','種別','案件ID','確認ID','パラメータ','状態','結果URL','メッセージ','処理開始','処理完了']。承認/却下をAppSheetのpending行アクションから作りやすいよう、設計の「パラメータ」に加え `確認ID` 列を明示追加。
> - 種別: `承認` / `却下`（確認ID必須・金額系は`アプリ権限`の管理者/経理のみ）、`カレンダー同期` / `Driveフォルダ`（案件ID必須・冪等）。
> - ★追加(2026-07-21): 種別 `請求申請` / `入金申請`（案件ID必須）。パラメータ列に JSON（`{"金額":1000000,"予定日":"2026-08-01","確定日":""}`）または `金額=1000000; 予定日=2026-08-01` 形式。projectsへは書かず pmQueuePending→経理グループへLINE通知（承認/却下）→承認で確定反映＝ダッシュボード/LINE報告と同一経路。依頼者(USEREMAIL())が申請者となり、承認確定時に projects の**最終更新者へ自動記録**（`アプリ権限`シートに表示名があればその名前、無ければメール）。
> - 承認/却下の中核は pm_pending.js から `pmApprovePendingCore`/`pmRejectPendingCore` としてUI非依存に抽出（LINE経路の挙動は不変）。Driveフォルダ作成は project_logs に記録（intent=app_job）。
> - 見積発行はアプリからも `見積ジョブ` に行追加で統一（pmジョブには持たせない）。

---

## 6. 権限（セキュリティ）

| 役割 | できること |
|---|---|
| 一般（現場/営業） | 閲覧＋フェーズ/次回アクション更新。金額は申請のみ |
| 経理 | 上記＋金額/請求/入金の承認・却下 |
| 管理者 | 全権＋辞書(会社/住所/グループ)編集 |

- USEREMAIL() ＋ `アプリ権限`マスタ（パイロット1と共通）で判定。
- 金額列は一般ユーザーには Read（編集UIを出さない）。Security Filter で担当案件のみ表示も可（任意）。

---

## 7. LINEとの併存（壊さない）

- LINEの「案件更新 〇〇、見積提出。6/25フォロー」等の**プレフィックス報告は従来どおり**。アプリはその結果を見る/整える場。
- LINE経由・アプリ経由のどちらの更新も **同じ projects/project_logs** に集約されるので、二重管理にならない。
- 競合（同時編集）は基本「後勝ち」。金額系は承認制なので事故りにくい。頻繁な同時編集が問題化したら列ロック/タイムスタンプ比較を検討（v1では不要）。

---

## 8. 構築手順（移行後・フェーズ1完了後）

1. フェーズ1ステップ1〜4（案件ID付与・バックフィル）完了を確認。
2. AppSheetでメインスプシ（移行後）を接続し、§2の6テーブルを追加・案件IDでRef設定。
3. §3のビュー作成（まず 案件ボード＋案件詳細＋ダッシュボード）。
4. §4の更新ルール（金額は申請、フェーズは直接）を Form/アクションで実装。
5. §5のジョブ枠（runPmJobs）をGASに追加（runEstimateJobsと共通設計）。
6. §6の権限設定。
7. テスト：1案件でフェーズ移動→ログ記録、金額申請→承認→確定、を確認。

---

## 9. 先に決めておきたいこと（次回相談）

1. **ボードの列**: フェーズ5列固定か、「失注/完了」を別レーンに畳むか。
2. **担当者で絞り込み**: 自分の案件だけ表示をデフォルトにするか（Security Filter）。
3. **ログの取り方**: アプリ変更ログを (a)AppSheet自動化 か (b)GASジョブ か（推奨b）。
4. **施工チェックリスト**: 進捗管理表(外部)をアプリに取り込むか、リンク表示に留めるか。

関連: `フェーズ1_整理プラン_案件ID統合.md` / `アプリ化方針_AppSheet_2026-06-30.md` / `AppSheet設計_見積ルール管理_v1.md` / `プロジェクト進捗管理_設計書.md`
