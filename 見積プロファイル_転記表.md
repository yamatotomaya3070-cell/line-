# 見積プロファイル 転記表（データ入力用）

作成: 2026-06-30 / 目的: 現在コードにハードコードされている見積設定値を `見積連携設定`（＝見積プロファイル）と `見積単価設定` シートへ転記するための一覧。
**この表を埋めても挙動は変わりません**（フェーズ0は読み取り口の用意まで）。実際に使われるのはフェーズ1の `est_core` 統合後。東邦は既存設定があるので**追記列のみ**、パイオニアは**新規プロファイル行**として用意します。

---

## 1. `見積連携設定`（見積プロファイル）に入れる値

列はヘッダー名で参照されるため、順不同でも可。空欄は従来デフォルトが適用される。

### 1-A. 東邦家具（既存 `toho` 行 — 追記列のみ埋める）

| 列 | 値 | 出所 |
|---|---|---|
| 設定ID | `toho` | 既存 |
| 表示名 | `東邦家具` | 既存 |
| 工程管理表ID | `1MHHVpkdNjTihse-CX4sOZbeGKi71ncKD` | estimate.js:849 |
| 工程シート名 | `工程管理表` | estimate.js:849 |
| 見積書ID | `1Fr5_KPcvTGa7WP7PpQ7glIQglaZgeuv_` | estimate.js:850 |
| テンプレートシート名 | `テンプレート` | estimate.js:850 |
| 有効 | `TRUE` | 既存 |
| **トリガーキーワード**（追記） | `東邦,東邦家具,toho` | （現状は表示名/IDで照合） |
| **レイアウト方式**（追記） | `fixed` | 座標固定（下記） |
| **税率**（追記） | `0.10` | TAX_RATE estimate.js:53 |
| **明細単位**（追記） | （空 or `枚`） | 単価設定側で指定 |
| **注記文言**（追記） | （テンプレ内固定なら空） | — |
| **出力先フォルダID**（追記） | （空＝テンプレスプシ内） | — |
| **紐付けプロジェクトID**（追記） | （案件単位にしたい場合のみ projects の案件ID） | 任意 |
| **発行方式**（追記） | `月別` | 対象月で工程を絞る |

> 東邦の `fixed` 座標（参考・現状ハードコード／estimate.js:18-53）:
> COVER_ITEM_START_ROW=14, COVER_MAX_ROWS=28, TOTAL_ROW=10, COVER_SUBTOTAL_ROW=43,
> DETAIL_START_ROW=53, DETAIL_PAGE2_END_ROW=84, DETAIL_PAGE3_START_ROW=92, DETAIL_PAGE3_END_ROW=126。
> ※フェーズ1で「座標もプロファイル列に外出し」する際の元データ。今は据え置き。

### 1-B. パイオニア什器（新規プロファイル行 — まるごと新規入力）

現在は `EST_PIONEER` にハードコードのみで `見積連携設定` には未登録。下記を1行追加して用意（フェーズ1のエンジンが読むまで挙動不変）。

| 列 | 値 | 出所 |
|---|---|---|
| 設定ID | `pioneer` | 新規 |
| 表示名 | `パイオニア什器` | 新規 |
| 工程管理表ID | `1AiI58tt-CLmlcgghsI_nyH3BVOn48LDr3erqNUR9gBA`（制作一覧スプシ） | estimate_pioneer.js:13 |
| 工程シート名 | `制作一覧` | estimate_pioneer.js:14 |
| 見積書ID | `1AITs5qf2A3-y5LLHN9hYi1mB4iWZ52F6FwyoIBBr2-s`（テンプレ） | estimate_pioneer.js:15 |
| テンプレートシート名 | `原本` | estimate_pioneer.js:16 |
| 有効 | `TRUE` | — |
| **トリガーキーワード** | `パイオニア,什器` | estimate_pioneer.js:595 |
| **レイアウト方式** | `auto` | detectEstimateLayout_ で自動検出 |
| **税率** | `0.10` | estimate_pioneer.js:17 |
| **明細単位** | `台` | estimate_pioneer.js:19（UNIT_LABEL） |
| **注記文言** | `※　見積もり項目に記載の無いものは別途となります。` | estimate_pioneer.js:359 |
| **出力先フォルダID** | （空＝マイドライブ直下） | estimate_pioneer.js:20 |
| **紐付けプロジェクトID** | （任意） | — |
| **発行方式** | `完了行一括` | 制作一覧の完了行をまとめて発行 |

---

## 2. `見積単価設定` に入れる値（東邦 `toho`）

| 設定ID | 分類 | 条件キーワード | 単価 | 単位 | 備考 |
|---|---|---|---|---|---|
| toho | 修正 | デフォルト | 5000 | H | 修正は時間単価 |
| toho | 新規 | デフォルト | 10000 | 枚 | 新規デフォルト |
| toho | 新規 | カウンター | 15000 | 枚 | カウンター系 |
| toho | 新規 | 建具 | 20000 | 枚 | 建具系 |

（出所: estimate.js:864-867 `setupEstimateForToho`。`setupEstimateForToho()` をGASで実行すれば自動投入される。）
パイオニアは制作一覧の「台数×単価」を直接読む方式のため、単価マスタは現状不要。

---

## 3. 申し送り（フェーズ1で対応）

1. **東邦seed行の列ズレ（既存・要確認）**: `setupEstimateForToho`（estimate.js:849-850）の `link.appendRow([...])` が9要素で、`担当者表示`列に `'YY-M'`、`単価`列に `'担当者：米田様'` が入る並び。readerは名前参照＋単価NaN時5000フォールバックのため実害は出にくいが、**本番`見積連携設定`シートの toho 行の各列値を一度目視確認**してほしい（正しい値が手入力済みなら問題なし）。
2. **トリガー外部化**: 現在の発火は estimate.js / estimate_pioneer.js のハードコード正規表現。`トリガーキーワード`列を使う形へはフェーズ1の `est_core` で移行（今は据え置き＝挙動不変）。
3. **座標の外出し**: 東邦の `fixed` 座標群をプロファイル列（またはレイアウト定義シート）へ。`auto`方式で吸収できるテンプレは座標入力不要。

関連: `機能調査・リファクタ提案_2026-06-30.md` / `リファクタ_ドメイン規約.md`
