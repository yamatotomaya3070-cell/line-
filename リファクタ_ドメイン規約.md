# 機能ドメイン規約（機能追加のルール）

作成: 2026-06-30 / 目的: 機能追加のたびに機能が入り乱れるのを防ぐ。
原則: **新しいトップレベル機能を増やさず、必ず既存の「大きな項目（ドメイン）」の中に足す。**

---

## 1. ドメイン（大きな項目）一覧

| ドメイン | 接頭辞 | 責務 | 主なファイル |
|---|---|---|---|
| 入口・ルーティング | `route_` | Webhook受信・イベント/コマンド振り分け | コード.js（doPost/handleEvent/handleDM/handleGroup/handlePostback） |
| 案件・進捗 | `pm_` | 案件マスタ(`projects`)・5フェーズ・進捗報告・確認待ち | pm_*.js／progress.js（施工チェックリストビュー） |
| タスク・予定 | `task_` | タスク管理シート・スケジュール・カレンダー | コード.js（writeTask/addToCalendar）／pm_taskbridge.js |
| 見積 | `est_` | 見積プロファイル→見積書生成 | estimate.js／estimate_pioneer.js（→ est_core へ統合予定） |
| アラート・通知 | `alert_` | 放置/請求漏れ/入金未確認・毎朝ダイジェスト | pm_alert.js／progress.js（ダイジェスト） |
| サマリー | `sum_` | 案件サマリーのDocs生成 | summary.js |
| 共通基盤 | `core_` | getSS/getSheet/callGemini/sendLineReply/identifyProject 等の共有処理 | コード.js（→ core_*.js へ抽出予定） |

## 2. 機能を追加するときのルール

1. **どのドメインに属すか先に決める**。該当が無ければ「本当に新ドメインが必要か」を設計書で検討（安易に増やさない）。
2. **トップレベル関数は接頭辞必須**（GASは全`.js`が同一グローバル名前空間。衝突防止のため）。
3. **データは設定/列の追加で吸収**。見積の新クライアント＝`見積連携設定`（見積プロファイル）に1行追加。進捗の新項目＝`PM_PHASES`／`projects`列を追加。**コード分岐を増やさない**。
4. **シート列の追加は非破壊で**。`setupSheet`／`ensureEstimateLinkSettingsSheet` は不足ヘッダーを右端へ名前判定で追加する。列はコード側でも**ヘッダー名参照**で読む（位置依存にしない）。
5. **ルーティングは先勝ち順序を意識**。新コマンドは判定順序（どの`if`より先か）を設計書に記し、誤発火（例: 見積と進捗質問の取り違え）を防ぐ。
6. **金額・請求・入金は自動確定しない**（既存ポリシー踏襲。確認待ち→承認）。

## 3. やってはいけないこと（過去の反省）

- 「完全独立」を理由に**同じ処理を別ファイルへコピペ**（例: estimate.js と estimate_pioneer.js の二重実装）。→ 共通処理は `core_`/`est_core` に寄せ、差分は**設定パラメータ**で吸収する。
- 同じ概念（案件・フェーズ・進捗）を**別シートに別形式で二重管理**。→ 案件の正は `projects`。他はそこから導出/名寄せ。
- 機能追加のために `コード.js` に直接ロジックを足してモノリスを膨らませる。→ ドメインのファイルへ。

## 4. 進行中の統合（2026-06-30 着手）

- 進捗: `projects`を正に一本化、`プロジェクト管理`は索引へ、`進捗管理表`は案件IDで名寄せ（施工チェックリストビュー）。
- 見積: `見積連携設定`を**見積プロファイル**へ拡張（トリガーキーワード/レイアウト方式/税率/単位/注記/出力先/紐付けプロジェクトID）。`est_core`へ統合予定。`projects`に`見積プロファイルID`列を追加済み（案件→プロファイル解決、無ければ会社単位にフォールバック）。

詳細・経緯: `機能調査・リファクタ提案_2026-06-30.md`
