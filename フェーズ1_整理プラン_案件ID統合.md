# フェーズ1 整理プラン｜案件IDで「二面一体」化（改訂版）

作成: 2026-06-30 / 方針: 既存を壊さない・かぶりをまとめる・アプリ(AppSheet)の土台にする。
実装は移行後に差分提示→push→検証で段階実施。本書は設計レベル。

---

## 0. なぜ「降格・撤廃」をやめ「案件IDリンク」にしたか（改訂理由）

当初案（`機能調査・リファクタ提案_2026-06-30.md`）では「projectsを唯一の正にし、プロジェクト管理を索引へ降格、二重登録を撤廃」としていた。
しかし実コードを精査した結果、`プロジェクト管理` は単なる索引ではなく**案件解決の心臓部**だと判明：

- `identifyProject()`（コード.js:696）/ `getProjectCandidates()`（778）/ `getActiveProjects()`（835）/ `matchProjectByName()`（759）が**すべて `プロジェクト管理` を位置参照**（[0]略称・[1]正式名称・[4]ステータス）。
- `getProjectNameByGroupId()`（2077）は**施主(col3)・業者(col4)の2グループID**で案件名を解決。
- `会社名`(col7)・`住所`(col8) は `answerAddressOrCompanyQuery()`（2505）/ `updateProjectField()`（2454）が使用。
- pm系統も `pmResolveProject()` → `identifyProject()` 経由で**この心臓部に依存**。
- 既に約35案件が稼働データとして登録済み（`bulkRegisterProjectsFromHistory` 参照）。

→ これを `projects` へ全面移管すると、Bot全体の案件解決を作り直すことになり**回帰リスクが極大**。
**安全策＝両シートを残し、役割を明確化して案件IDで結ぶ**（＝二面一体）。かぶり（正が不明）の問題は解消しつつ、心臓部は無傷。

---

## 1. 2シートの役割確定

| シート | 役割（正とするもの） | 主な列 | 触り方 |
|---|---|---|---|
| `プロジェクト管理` | **辞書**：名前解決・グループ紐付け・会社/住所マスタ | 略称, 正式名称, グループID（施主）, グループID（業者）, ステータス, 備考, 会社名, 住所 | 列追加のみ（案件ID）。ロジックは維持 |
| `projects` | **進捗**：フェーズ・金額・請求・入金・次回アクション | 案件ID, 案件名, クライアント名, 担当者, 現在フェーズ, 各ステータス, 金額系, 日付系… | 既存（フェーズ0で見積プロファイルID追加済み） |
| `進捗管理表`(外部) | **施工チェックリスト**（A系統・読取） | 店舗名, 施工月, 図面チェック… ＋(追加)案件ID | 外部実務シート。手動で案件ID列追加 |

3シートが **案件ID** で串刺しになる。

---

## 2. 案件IDで結ぶ設計

- **採番**: 既存 `pmGenProjectId()`（`YYYY-NNN`）を唯一の採番元とする。
- **付与先**: `projects.案件ID`（既存）＋ `プロジェクト管理.案件ID`（新規列）＋ `進捗管理表.案件ID`（新規列）。
- **単一作成経路**: 新規案件は `pmEnsureProjectRecord()`（pm_manager.js:40）を「唯一の入口」とし、ここで
  1. `projects` に行作成（案件ID採番）
  2. `registerNewProject()` で `プロジェクト管理` にも行作成 → **同じ案件IDを書き込む**（現状は書いていない＝ここを追加）
  - ＝二重“管理”ではなく、二面に**同一IDを刻む**だけ。撤廃しない。
- **既存タスク経路の整合**: `writeTask()` 等から `registerNewProject()` が単独で呼ばれる場合も、案件IDを採番/再利用して付与（名前一致があればそのIDを使う）。

---

## 3. 非破壊の実施ステップ（移行後）

| 順 | 内容 | 破壊性 | 実装先 |
|---|---|---|---|
| 1 | `プロジェクト管理` に `案件ID` 列を追加（setup の headers に追記＝右端非破壊） | なし | コード.js setup headers / 3294行の定義 |
| 2 | バックフィル関数 `backfillProjectIds()`：既存 `プロジェクト管理` 各行に案件IDを採番付与。`projects` に同名行が無ければ最小行を作成し同一IDを付与（名前一致で突合） | なし（追記のみ・冪等） | 新規 util |
| 3 | `registerNewProject()` を案件ID対応に拡張（引数で受けた/採番したIDを書く。既存呼び出しは後方互換） | 低（列追加前提・既存挙動維持） | コード.js:2108 |
| 4 | `pmEnsureProjectRecord()` が projects と プロジェクト管理 に**同一案件ID**を書くよう接続 | 低 | pm_manager.js:40 |
| 5 | `進捗管理表`(外部) に手動で `案件ID` 列追加＋既存行に記入（実務シートなのでコードは触らない） | なし（手動） | スプシ手動 |
| 6 | （任意）名前解決の重複ロジックを `core_status`/`core_project` へ切り出し（identifyProject等は中身を移すだけで挙動不変） | 中（要テスト・後回し可） | 新規 core_*.js |

ステップ1〜4が「案件ID二面一体」の本体。5は実務シート連携。6は整理の仕上げで**任意・後回し可**。

---

## 4. ルーティング非対称の扱い（データ統合とは独立）

調査で判明したA/B進捗ルーティングの非対称（DM=A先→B、グループ非メンション=Bのみ、メンション=Aのみ）は、**データ統合とは別問題**。安全に分けて対応：

- **方針（おすすめ）**: 「グループは無反応・メンション時のみ反応」という既存ポリシーは**維持**（過剰反応抑制は良い設計）。ただしメンション時に **A（進捗質問）とB（pmプレフィックス）の両方**を判定できるよう、`handleMentionCommand` に `pmRouteText` も通す（現状Bが無い）。
- これは1〜数行の追加で、非該当時は即false＝既存を壊さない。
- 実施はデータ統合の後、独立ステップとして。

---

## 5. アプリ（パイロット2 進捗ボード）との接続

- AppSheetは `projects` を主テーブルにし、`プロジェクト管理`(辞書) と `案件ID` でRef結合。
- 案件カード = projects(フェーズ/金額/次回) ＋ プロジェクト管理(会社名/住所/グループ) ＋ 進捗管理表(施工チェック) を1画面に。
- フェーズ別カンバン＝projects.現在フェーズ。承認＝project_pending_updates。
- ＝**フェーズ1ステップ1〜4が終われば、パイロット2はきれいに繋がる**。

---

## 6. 残すリスク / 将来

- 当面、辞書(プロジェクト管理)と進捗(projects)は2シートのまま。完全な1シート統合は**任意・将来**（必要になったら）。今やる必要はなく、リスクを取らない。
- バックフィルは名前一致で突合するため、表記ゆれがある案件は手動確認リストを出す（`backfillProjectIds()` が不一致を報告）。

関連: `機能調査・リファクタ提案_2026-06-30.md`（§Step2,4・本書で改訂）/ `アプリ化方針_AppSheet_2026-06-30.md` / `リファクタ_ドメイン規約.md`
