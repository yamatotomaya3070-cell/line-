# WOODBASE LINE秘書AI 要件定義書

作成日: 2026-05-14  
対象システム: WOODBASE LINE秘書AI  
対象コード: Google Apps Script / LINE Messaging API / Google Workspace 連携

---

## 1. 本書の目的

本書は、完成済みコードをもとに、WOODBASE LINE秘書AIシステムの要件・仕様・運用方法を第三者へ引き継げる形で整理した要件定義書である。

開発者・運用担当者・保守担当者が以下を理解できることを目的とする。

- システムが何を解決するものか
- LINE上でユーザーがどのように使うか
- どの外部サービス・スプレッドシート・設定値に依存しているか
- どの機能がどのデータを読み書きするか
- 新規案件追加・障害調査・再デプロイ時に何を確認すべきか

---

## 2. システム概要

### 2.1 コンセプト

WOODBASEのメンバーは、従来どおりLINEで会話するだけでよい。  
Botが会話内容からタスク・予定・進捗・見積書・案件サマリーを自動で管理する。

### 2.2 主な利用者

| 利用者 | 利用内容 |
|---|---|
| 社内メンバー | タスク登録、残タスク確認、予定確認、完了報告、進捗確認、見積書作成 |
| 現場・案件グループ参加者 | LINEグループ上で通常会話、必要時のみBot呼び出し |
| 管理者 | スプレッドシート設定、GASデプロイ、LINE Webhook管理、権限管理 |
| 保守担当者 | コード修正、障害調査、トリガー・ログ確認 |

### 2.3 使用サービス

| サービス | 用途 |
|---|---|
| LINE Messaging API | BotとのDM・グループ会話・返信・Push通知 |
| Google Apps Script | Webhook受信、業務ロジック実行、定期トリガー |
| Google Sheets | タスク、予定、メンバー、案件、ログ、見積設定の管理 |
| Google Calendar | 予定登録 |
| Google Drive | 画像・PDF・動画・Docs保存 |
| Google Docs | 案件サマリー自動生成 |
| Gemini API | 自然文からのタスク・予定抽出、会話要約、文脈回答 |
| clasp | ローカルコードとGASの同期 |

### 2.4 主要ファイル

| ファイル | 役割 |
|---|---|
| `コード.js` | LINE Webhook、タスク、予定、ファイル保存、メンバー管理、トリガー、共通処理 |
| `progress.js` | 進捗管理表への自然文クエリ |
| `estimate.js` | 工程管理表から見積書シートを自動生成 |
| `summary.js` | 案件サマリーをGoogle Docsへ生成・更新 |
| `migrate.js` | アカウント移行、仮タスク移行などの運用補助 |
| `appsscript.json` | GAS権限、タイムゾーン、Webアプリ設定 |
| `WOODBASE LINE秘書AI 機能一覧.md` | 利用者向け機能一覧 |

---

## 3. 全体処理フロー

### 3.1 LINE Webhook受信

1. LINEからGAS Webアプリの `doPost(e)` にイベントが送信される。
2. `verifyLineWebhook(e)` でWebhook検証を行う。
3. イベント配列を1件ずつ `handleEvent(ev)` に渡す。
4. イベント種別により以下へ分岐する。

| イベント種別 | 処理 |
|---|---|
| `follow` | メンバー自動登録 |
| `join` | グループ参加時の案件選択UI表示 |
| `memberJoined` | 参加メンバーの自動登録 |
| `postback` | Quick Reply / ボタン操作処理 |
| `message:image/file/video` | Driveへ自動保存 |
| `message:text` | DMまたはグループ処理へ分岐 |

### 3.2 DM処理

1. メンバー未登録の場合は登録し、初回案内を返信する。
2. 見積設定一覧コマンドを判定する。
3. 見積書作成依頼を判定する。
4. 進捗管理表への質問を判定する。
5. 完了報告を判定する。
6. 会話要約依頼を判定する。
7. いずれにも該当しない場合は、個人向け照会回答を返す。

### 3.3 グループ処理

グループでは、通常会話にBotが反応しすぎないことを重視する。

| 入力状態 | Botの動作 |
|---|---|
| 通常メッセージ | メッセージログのみ保存し、返信しない |
| Botメンション | コマンドまたは自然文依頼として処理 |
| テキストプレフィックス | Botメンション扱いとして処理 |

対応プレフィックス:

- `@WBG`
- `＠WBG`
- `/WBG`
- `/wbg`
- `秘書:`
- `秘書：`

---

## 4. 機能要件

### FR-001 メッセージログ保存

LINEグループで投稿されたテキストメッセージを `メッセージログ` シートに保存する。

| 項目 | 内容 |
|---|---|
| 入力 | LINEテキストメッセージ |
| 出力 | `メッセージログ` への追記 |
| 保存項目 | 日時、グループID、送信者、メッセージ |
| 備考 | グループ通常会話では返信しない |

### FR-002 タスク自動抽出・登録

Bot宛メンションまたはDMでタスク依頼文を受け取った場合、Geminiでタスク情報を抽出し、スプレッドシートへ登録する。

| 項目 | 内容 |
|---|---|
| 入力例 | `濱田さん 雨晴れクリニックの見積もりを4月25日までにお願いします` |
| 抽出項目 | 案件名ヒント、担当者、タスク内容、期日、緊急度 |
| 登録先 | `タスク管理` |
| 初期ステータス | `confirmed` |
| 通知 | 担当者DM、必要に応じて社内グループ通知 |

除外対象:

- 了解しました
- 承知しました
- ありがとうございます
- お疲れ様です
- 確認しました

### FR-003 プロジェクト保存先選択

未紐付けグループでタスク・予定が抽出された場合、AIが案件を自動確定せず、候補UIを表示してユーザーに選択させる。

処理フロー:

1. グループIDから既存案件との紐付けを確認する。
2. 紐付け済みの場合は、その案件へ即登録する。
3. 未紐付けの場合は、候補案件を最大4件表示する。
4. 候補に加えて「新規プロジェクト作成」を表示する。
5. ユーザー選択後、仮タスクを本登録する。

### FR-004 新規プロジェクト作成

ユーザーが「新規プロジェクト作成」を選択した場合、次のメッセージをプロジェクト名として受け取り、`プロジェクト管理` に登録する。

| 項目 | 内容 |
|---|---|
| 入力 | 新規プロジェクト名 |
| 入力期限 | 10分以内 |
| キャンセル | `キャンセル` で中止 |
| 登録先 | `プロジェクト管理` |
| 後続処理 | 仮タスク・予定を新規案件に紐付けて本登録 |

### FR-005 緊急度自動判定

タスク文に緊急キーワードが含まれる場合、緊急度を `高` として登録する。

緊急キーワード:

- 至急
- 急ぎ
- 急いで
- ASAP
- 今すぐ
- 今日中
- 本日中
- 明日まで
- 最短
- 緊急
- GW前
- GW中

緊急タスクは通知文に `至急` 表示を付ける。

### FR-006 重複タスク検出

同一グループ・同一担当者・同一内容のタスクが48時間以内に存在する場合、二重登録を防止する。

### FR-007 タスク完了処理

DMまたはBotメンションで完了報告を受け取った場合、対象タスクを推定し、ステータスを完了へ更新する。

入力例:

- 完了しました
- 終わりました
- 対応済みです

### FR-008 タスク・予定照会

ユーザーの自然文質問に対して、タスク・予定の一覧を返信する。

入力例:

- 残タスクは？
- 今週の予定は？
- ○○案件の残タスクは？
- 仮タスク

### FR-009 スケジュール自動登録

日時・場所・参加者を含むメッセージから予定を抽出し、`スケジュール管理` とGoogleカレンダーに登録する。

| 項目 | 内容 |
|---|---|
| 入力例 | `4月18日14時から雨晴れクリニックで現場確認です` |
| 登録先 | `スケジュール管理`、Google Calendar |
| 不足時 | いつ・どこで・誰と、などの補足質問を返す |

### FR-010 ファイル自動保存

LINEグループに送信された画像・PDF・動画をGoogle Driveへ保存する。

| 項目 | 内容 |
|---|---|
| 対象 | `image`、`file`、`video` |
| 保存先 | 案件フォルダ、または未分類フォルダ |
| 案件判定 | グループIDとプロジェクト管理の紐付けを利用 |

### FR-011 メンバー管理

友だち追加またはグループ参加時に、LINEユーザーを `メンバー管理` に登録する。

用途:

- タスク担当者へのDM通知
- 送信者名の解決
- 社内メンバー管理

### FR-012 進捗管理表 自然文クエリ

LINEから進捗管理表に対して自然文で質問できる。

入力例:

- 図面チェックできてない店舗ある？
- 5月の図面チェックまだのところ教えて
- 確認中の店舗だけ教えて
- 6月施工で未完了ある？
- 田中さん担当で未完了ある？

処理内容:

1. `PROGRESS_SPREADSHEET_ID` が設定されていれば外部スプレッドシートを開く。
2. `PROGRESS_SHEET_NAME_OVERRIDE` または既定名 `進捗管理表` のシートを取得する。
3. ヘッダー行を自動検出する。
4. 質問文から対象列、月、店舗、担当者、状態を抽出する。
5. 未完了・要確認項目を最大30件まで返信する。

完了扱い:

- 丸記号各種
- 済
- 済み
- 完了
- OK
- done
- チェックマーク
- 日付列に値がある場合

未完了扱い:

- 空欄
- 確認中
- 対応中
- 保留
- 差戻し
- 要確認
- `△`
- `×`

### FR-013 見積書 自動作成

工程管理表から対象月の見積書シートを自動生成する。

入力例:

- 東邦家具 5月分の見積書作って
- toho 5月分 見積作成
- 〇〇家具 2026年6月の見積書作成
- 見積設定一覧

処理フロー:

1. `見積連携設定` から対象設定を検索する。
2. メッセージから対象年月を抽出する。
3. 見積書スプレッドシートに同名シートがないか確認する。
4. 工程管理表から対象月の行を取得する。
5. 物件単位にグループ化する。
6. 金額を計算する。
7. テンプレートシートをコピーして月別シートを作成する。
8. 表紙・明細・合計欄へ値を書き込む。
9. 自動処理できない行を `見積要確認` に記録する。
10. LINEへ作成結果と見積書URLを返信する。

月別シート名:

- `2026年5月` → `26-5`
- `2026年12月` → `26-12`

金額計算:

| 分類 | 数量 | 単位 | 単価欄 | 金額 |
|---|---:|---|---|---:|
| 修正 | 製図時間 | H | 1H時は空欄、2H以上は単価 | 製図時間 × 単価 |
| 新規 | 枚数 | 枚 | 空欄 | 製図時間 × 単価 |

要確認扱い:

- 製図時間が空欄
- 分類が空欄
- 分類が `検討のみ`
- 分類が `WBF`
- 未対応の分類

### FR-014 案件サマリー Docs更新

LINEから案件サマリー作成・更新を依頼できる。

入力例:

- 案件まとめて
- Docs更新
- サマリー作成
- 要約して

処理内容:

1. 案件を推定する。
2. 不明な場合は案件選択UIを表示する。
3. メッセージログ、タスク、予定を収集する。
4. Geminiで案件サマリーを生成する。
5. Google Docsに最新サマリー、差分、履歴を保存する。

Docs保存先:

- `DRIVE_FOLDER_ID` 配下
- サブフォルダ名: `案件サマリー`

### FR-015 文脈回答

Botメンション時に明確なコマンドへ該当しない場合、直近の会話履歴・未完了タスク・予定をもとにGeminiで簡潔に回答する。

制約:

- 丁寧語
- 3〜4文以内
- コマンド一覧のような定型返答は避ける
- 不明な場合は確認事項を具体的に返す

### FR-016 定期実行

GASトリガーにより定期処理を実行する。

| タイミング | 関数 | 処理 |
|---|---|---|
| 毎日7時 | `runDailySummaryUpdate` | 進行中案件のDocsサマリー更新 |
| 毎日9時 | `checkDeadlines` | 期日リマインド、連絡漏れ検出 |
| 毎週月曜8時 | `sendWeeklyReport` | 週次レポート |
| 毎週日曜23時 | `archiveCompletedTasks` | 完了タスクをアーカイブ |

---

## 5. データ要件

### 5.1 管理用スプレッドシート

`SPREADSHEET_ID` で指定されるメインスプレッドシートに以下のシートを持つ。

| シート名 | 用途 |
|---|---|
| タスク管理 | 未完了・進行中タスクの管理 |
| 完了タスク | 完了済みタスクのアーカイブ |
| スケジュール管理 | 予定一覧 |
| 仮タスク | 案件選択前・確認待ちの一時保存 |
| メンバー管理 | LINEユーザーIDと名前の管理 |
| プロジェクト管理 | 案件名、略称、グループID紐付け |
| メッセージログ | グループ会話履歴 |
| 見積連携設定 | 工程管理表と見積書の対応設定 |
| 見積単価設定 | 見積単価ルール |
| 見積要確認 | 見積自動処理できなかった行の記録 |

### 5.2 タスク管理

| 列 | 内容 |
|---|---|
| タスクID | システム生成ID |
| 案件名 | プロジェクト名 |
| タスク内容 | 抽出された作業内容 |
| 担当者 | 担当者名 |
| 期日 | 期限 |
| ステータス | `pending` / `confirmed` / `done` |
| 作成日時 | 登録日時 |
| グループID | 発生元LINEグループ |
| 緊急度 | 通常または高 |

### 5.3 スケジュール管理

| 列 | 内容 |
|---|---|
| 登録日時 | 登録日時 |
| 案件名 | プロジェクト名 |
| 予定タイトル | 予定名 |
| 日付 | 予定日 |
| 開始時間 | 開始時刻 |
| 終了時間 | 終了時刻 |
| 場所 | 場所 |
| 参加者 | 参加者 |
| 詳細 | 補足 |
| グループID | 発生元LINEグループ |

### 5.4 プロジェクト管理

| 列 | 内容 |
|---|---|
| 略称 | 案件検索・推定用の短縮名 |
| 正式名称 | 案件名 |
| グループID（施主） | LINEグループID |
| グループID（業者） | LINEグループID |
| ステータス | 進行中など |
| 備考 | 任意メモ |

### 5.5 見積連携設定

| 列 | 内容 |
|---|---|
| 設定ID | 半角英数の識別子 |
| 表示名 | LINEで指定する案件名 |
| 工程管理表ID | 工程管理表スプレッドシートID |
| 工程シート名 | 工程管理表のタブ名 |
| 見積書ID | 見積書スプレッドシートID |
| テンプレートシート名 | コピー元テンプレート |
| 担当者表示 | 見積書備考欄等に出す担当者表記 |
| 単価 | 1時間あたり単価。空欄時は5000 |
| 有効 | `TRUE` のみ利用 |
| 備考 | 任意メモ |

---

## 6. 外部設定値

GASのScript Propertiesに以下を設定する。

| キー | 必須 | 内容 |
|---|---|---|
| `LINE_CHANNEL_ACCESS_TOKEN` | 必須 | LINE Messaging APIのアクセストークン |
| `LINE_CHANNEL_SECRET` | 推奨 | LINE Channel Secret |
| `GEMINI_API_KEY` | 必須 | Gemini APIキー |
| `SPREADSHEET_ID` | 必須 | 管理用スプレッドシートID |
| `INTERNAL_GROUP_ID` | 任意 | 社内通知先LINEグループID |
| `DRIVE_FOLDER_ID` | 必須 | ファイル・Docs保存先DriveフォルダID |
| `CALENDAR_ID` | 必須 | 予定登録先GoogleカレンダーID |
| `PROGRESS_SPREADSHEET_ID` | 任意 | 進捗管理表の外部スプレッドシートID |
| `PROGRESS_SHEET_NAME_OVERRIDE` | 任意 | 進捗管理表のシート名 |
| `BOT_USER_ID` | 自動 | Bot自身のLINEユーザーIDキャッシュ |

---

## 7. 非機能要件

### 7.1 セキュリティ

- LINE Webhookは `LINE_CHANNEL_SECRET` を用いた検証に対応する。
- GASの制約上、HTTPヘッダー `X-Line-Signature` を直接取得できないため、URLクエリの `?token=` または `?sig=` で検証する。
- `?token=` が設定された場合は厳密照合する。
- トークンやAPIキーはコードへ直書きせず、Script Propertiesで管理する。

### 7.2 データ保護

- `setup()` は既存データを削除しない。
- 既存シートに不足列がある場合は右端へ追加する。
- 破壊的初期化は `resetAllSheetsDangerously()` に分離され、確認UIを表示する。
- 見積書の月別シートが既に存在する場合は上書きせず処理を中止する。

### 7.3 可用性・運用性

- グループ通常会話では返信しないため、Botの過剰反応を防ぐ。
- Gemini失敗時、案件サマリーは簡易フォールバックを返す。
- 見積書作成時に自動処理できない行は処理を止めず、`見積要確認` に記録する。

### 7.4 保守性

- 新しい見積対象は `見積連携設定` に1行追加するだけで対応できる。
- 進捗管理表は外部スプレッドシートIDとシート名をプロパティで切り替えられる。
- LINEプレフィックスによりPC版LINEでメンションできない場合も運用できる。

---

## 8. 初期セットアップ手順

### 8.1 GASプロパティ設定

1. GASエディタを開く。
2. Script Propertiesに必要なキーを登録する。
3. `SPREADSHEET_ID`、`DRIVE_FOLDER_ID`、`CALENDAR_ID` の権限を実行アカウントに付与する。

### 8.2 シート初期化

1. GASエディタで `setup()` を実行する。
2. 必要な管理シートが作成される。
3. 既存データがある場合は削除されず、不足列のみ追加される。

### 8.3 見積関連シート作成

1. `setupEstimateSheets()` を実行する。
2. `見積連携設定`、`見積単価設定`、`見積要確認` が作成される。
3. 必要に応じて `見積連携設定` に対象案件を追加する。

### 8.4 進捗管理表設定

外部スプレッドシートを使う場合:

1. `setupProgressSpreadsheet(spreadsheetId, sheetName)` を実行する。
2. 対象スプレッドシートをGAS実行アカウントへ共有する。
3. `showProgressSpreadsheet()` で接続先を確認する。

### 8.5 トリガー設定

1. `createTriggers()` を実行する。
2. `setupSummaryTrigger()` を実行する。
3. 初回実行時にOAuth承認を行う。

### 8.6 LINE Webhook設定

1. GASをWebアプリとしてデプロイする。
2. LINE Developers ConsoleのWebhook URLにGAS WebアプリURLを設定する。
3. 推奨運用ではURL末尾に `?token=<LINE_CHANNEL_SECRET>` を付ける。
4. Webhook利用を有効化する。

---

## 9. 運用手順

### 9.1 新しいLINEグループを使い始める

1. LINEグループにBot `@520goeco` を招待する。
2. Botが案件選択UIを表示する。
3. 既存案件を選ぶ、または新規案件を作成する。
4. 以後、そのグループのタスク・予定は紐付いた案件へ登録される。

### 9.2 新しい見積対象を追加する

1. 工程管理表と見積書を `woodbasegroup@gmail.com` に編集者共有する。
2. `見積連携設定` に1行追加する。
3. LINEで `見積設定一覧` を送って表示されるか確認する。
4. `表示名 5月分の見積書作って` の形式でテストする。

### 9.3 進捗管理表を変更する

1. 新しい進捗管理表をGAS実行アカウントへ共有する。
2. `setupProgressSpreadsheet(id, sheetName)` を実行する。
3. `diagnoseProgressSheet()` でヘッダー検出を確認する。
4. LINEから `未完了全部教えて` などで動作確認する。

### 9.4 再デプロイ

1. ローカルコードをGASへ反映する。
2. GASのデプロイ管理から新バージョンを作成する。
3. WebアプリURLが変わった場合はLINE Webhook URLを更新する。
4. OAuthスコープ追加後は再承認する。

---

## 10. 受け入れ基準

### 10.1 LINE基本動作

- DMでは通常メッセージにBotが反応すること。
- グループ通常会話ではBotが返信しないこと。
- グループで `@WBG` または `/WBG` を付けるとBotが反応すること。

### 10.2 タスク管理

- 自然文から担当者・内容・期日を抽出して登録できること。
- 未紐付けグループでは案件候補UIが表示されること。
- 新規プロジェクト作成後、仮タスクが正しい案件に登録されること。
- 緊急ワードを含むタスクに緊急度が付くこと。
- 完了報告で対象タスクが完了になること。

### 10.3 予定管理

- 日時を含むメッセージから予定が登録されること。
- 必要情報が不足する場合、補足質問が返ること。
- Googleカレンダーにも予定が作成されること。

### 10.4 進捗管理表

- `図面チェックできてない店舗ある？` に対して未完了店舗を返すこと。
- 月・担当者・店舗・状態で絞り込めること。
- タスク依頼文を進捗質問と誤判定しないこと。

### 10.5 見積書作成

- `見積設定一覧` で有効な設定が表示されること。
- 対象月の見積書シートがテンプレートから作成されること。
- 既存シートがある場合は上書きしないこと。
- 小計・消費税・税込合計が反映されること。
- 要確認行が `見積要確認` に記録されること。

### 10.6 Docsサマリー

- 案件サマリーがDocsに作成または更新されること。
- 最新サマリー、差分、履歴のセクションが維持されること。
- 日次トリガーで進行中案件が更新されること。

---

## 11. 動作確認用関数

| 関数 | 確認内容 |
|---|---|
| `testVerifyLineSignature()` | Webhook署名検証 |
| `testSafeSetupDoesNotDeleteData()` | `setup()` が既存データを消さないこと |
| `testTaskHeadersIncludeUrgency()` | タスク管理・完了タスクの列構成 |
| `testPostbackRouting()` | postback分岐 |
| `testSyncAllGroupMembersNoDuplicate()` | メンバー同期関数 |
| `testProgressQuestion()` | 進捗質問の回答 |
| `diagnoseProgressSheet()` | 進捗管理表のヘッダー検出 |
| `testEstimateDryRun()` | 見積書作成の事前確認 |
| `diagnoseEstimateTemplateForToho()` | 見積テンプレート構造確認 |
| `testGenerateSummary()` | Docsサマリー生成 |
| `testDocsConnection()` | Docs保存先フォルダ確認 |

---

## 12. 障害調査ポイント

### Botが反応しない

確認項目:

1. GAS WebアプリURLがLINE Webhookに設定されているか。
2. Webhook利用が有効か。
3. `LINE_CHANNEL_ACCESS_TOKEN` が正しいか。
4. `LINE_CHANNEL_SECRET` とURLの `?token=` が一致しているか。
5. GASの実行ログにエラーが出ていないか。

### タスクが登録されない

確認項目:

1. グループでBotメンションまたはプレフィックスを付けているか。
2. Gemini APIキーが有効か。
3. `SPREADSHEET_ID` のシート権限があるか。
4. `仮タスク` に保存されていないか。
5. 48時間以内の重複としてスキップされていないか。

### 進捗質問に答えない

確認項目:

1. `PROGRESS_SPREADSHEET_ID` が正しいか。
2. `PROGRESS_SHEET_NAME_OVERRIDE` が実際のシート名と一致しているか。
3. 対象スプレッドシートが共有されているか。
4. `diagnoseProgressSheet()` でヘッダー行が見つかるか。

### 見積書が作れない

確認項目:

1. `見積連携設定` に対象設定があるか。
2. `有効` が `TRUE` になっているか。
3. 工程管理表ID、見積書ID、テンプレート名が正しいか。
4. 対象月のデータが工程管理表にあるか。
5. 同名シートが既に存在していないか。
6. スプレッドシートがGAS実行アカウントに共有されているか。

### Docsサマリーが作れない

確認項目:

1. `DRIVE_FOLDER_ID` が設定されているか。
2. Driveフォルダに編集権限があるか。
3. `documents` OAuthスコープの再承認が済んでいるか。
4. 案件に紐付くメッセージ・タスク・予定が存在するか。

---

## 13. 今後の拡張候補

- Webhook署名検証をCloudflare Worker等の前段で完全実施する。
- 管理者向けの設定確認コマンドを追加する。
- 見積テンプレートのセル位置を設定シート化する。
- タスク担当者名の表記ゆれ辞書を追加する。
- エラー通知を社内グループへPushする。
- テスト関数を自動実行できる保守チェックリストにまとめる。

---

## 14. 引き継ぎ時に必ず共有するもの

- GASプロジェクトのURL
- LINE Developers Consoleの権限
- 管理用スプレッドシートURL
- Drive保存先フォルダURL
- GoogleカレンダーID
- Gemini APIキーの管理場所
- `SPREADSHEET_ID` などScript Propertiesの設定一覧
- clasp / GitHubリポジトリ情報
- 本番Webhook URL
- 現在有効なGASデプロイバージョン

