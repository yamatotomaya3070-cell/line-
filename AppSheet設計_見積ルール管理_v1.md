# AppSheet設計図｜パイロット1：見積ルール管理アプリ（v1）

作成: 2026-06-30 / 目的: スプシを直接触らず、見積ルール（プロファイル・単価）をUIで設定し、ボタンで見積書を生成する。
前提: データはメインスプシのまま。帳票生成はGASに残し、アプリのボタンから呼ぶ。構築はWorkspace移行後のスプシ上で。

---

## 1. アプリ概要

| 項目 | 内容 |
|---|---|
| アプリ名 | 見積ルール管理（仮） |
| 利用者 | 管理者（ルール編集・見積発行）／一般（閲覧・発行のみ） |
| デバイス | PC＋スマホ（AppSheet両対応） |
| データ源 | メインスプシの 4 シート（下記） |
| やること | ①プロファイル設定 ②単価表編集 ③「見積作成」ボタンで発行 ④結果（URL）確認 |

---

## 2. テーブル設計（既存シート＋ジョブ用1枚）

### 2-1. `見積連携設定`（=見積プロファイル）★主テーブル
キー: `設定ID`

| 列 | AppSheet型 | 入力UI | 備考 |
|---|---|---|---|
| 設定ID | Text（Key） | テキスト | 例 toho / pioneer。一意 |
| 表示名 | Text | テキスト | 例 東邦家具 |
| 有効 | Yes/No | トグル | OFFで一覧から除外 |
| トリガーキーワード | Text | テキスト | カンマ区切り「パイオニア,什器」 |
| 工程管理表ID | Text | テキスト | スプシID（参照元） |
| 工程シート名 | Text | テキスト | |
| 見積書ID | Text | テキスト | テンプレ/出力先スプシID |
| テンプレートシート名 | Text | テキスト | |
| レイアウト方式 | Enum（auto/fixed） | ドロップダウン | 既定 auto |
| 税率 | Decimal | 数値 | 既定 0.10 |
| 明細単位 | Enum（枚/台/式/H…） | ドロップダウン | |
| 注記文言 | LongText | 複数行 | |
| 出力先フォルダID | Text | テキスト | 空=既定 |
| 担当者表示 | Text | テキスト | 見積書の担当欄 |
| 紐付けプロジェクトID | Ref→projects（任意） | 参照 | 案件単位にしたい時のみ |
| 備考 | LongText | 複数行 | |
| 単価（旧・既定値） | Number | 数値 | 後方互換。基本は単価表で管理 |

関連: `見積単価設定`（1対多。設定ID で子行を表示）。

### 2-2. `見積単価設定`（プロファイルごとの単価行）
キー: 自動採番（または 設定ID+分類+条件キーワード の複合）

| 列 | AppSheet型 | 備考 |
|---|---|---|
| 設定ID | Ref→見積連携設定 | 親プロファイル |
| 分類 | Enum（修正/新規/…） | |
| 条件キーワード | Text | 例 デフォルト/カウンター/建具 |
| 単価 | Number | |
| 単位 | Enum（枚/H/台/式） | |
| 備考 | Text | |

> ※キー列が無いシートはAppSheetが警告するため、構築時に先頭へ `行ID`（UNIQUEID）列を1本足すのが安全（非破壊・右端でも可）。

### 2-3. `見積要確認`（自動処理できなかった行のログ・読み取り中心）
| 列 | 型 | 備考 |
|---|---|---|
| 日時 / 設定ID / シート名 / 物件 / 図面名 / 分類 / 理由 | Text/DateTime | estimate.js が追記。アプリでは一覧表示して「未対応の取りこぼし」を可視化 |

### 2-4. `見積ジョブ`（新規・アプリ↔GAS連携用キュー）★要追加
キー: `ジョブID`（UNIQUEID）

| 列 | 型 | 用途 |
|---|---|---|
| ジョブID | Text(Key) | 自動採番 |
| 設定ID | Ref→見積連携設定 | どのプロファイルで作るか |
| 対象月 | Text/Enum | 例 2026-05（「5月分」に相当） |
| 上書き | Yes/No | 既存シートを上書きするか |
| 状態 | Enum（受付/処理中/完了/エラー） | GASが更新 |
| 結果URL | URL | 生成された見積書リンク（GASが書込） |
| メッセージ | LongText | エラー内容など |
| 依頼者 | Text | AppSheet USEREMAIL() |
| 依頼日時 | DateTime | |

---

## 3. ビュー設計

| ビュー | 種類 | 内容 |
|---|---|---|
| プロファイル一覧 | Table/Deck | 表示名・有効・税率・単位。タップで詳細へ |
| プロファイル詳細 | Detail | 全項目＋子テーブル「単価表」をインライン表示＋「見積作成」ボタン |
| プロファイル編集 | Form | 追加/編集フォーム（Enum・トグルで迷わず入力） |
| 単価表編集 | Table（inline編集） | 親プロファイル配下の単価行を追加/編集 |
| 見積作成 | Form | 対象月・上書きを選んで実行（=見積ジョブを1行作成） |
| ジョブ状況 | Table/Deck | 状態（受付/処理中/完了/エラー）と結果URLを表示 |
| 要確認ログ | Table（read） | 取りこぼし行の確認 |

---

## 4. 「見積作成」ボタンの仕組み（ジョブキュー方式・推奨）

エンドポイント公開や秘密鍵管理が要らず、ノーコードで堅牢な方式：

```
[アプリ] プロファイル詳細の「見積作成」ボタン
   → AppSheetアクション: 「見積ジョブ」に1行追加（設定ID/対象月/上書き/状態=受付/依頼者）
        │
        ▼
[GAS] 時間トリガー（毎分）または onChange で runEstimateJobs() を実行  ← ★新規追加が必要
   → 状態=受付 のジョブを取得 → 状態=処理中
   → 設定IDから既存の見積生成（createEstimateFromProcessSheet / パイオニアは別ルート）を呼ぶ
   → 成否を ジョブ行に書き戻し（状態=完了/エラー、結果URL、メッセージ）
        │
        ▼
[アプリ] 「ジョブ状況」ビューで状態と結果URLを確認（数十秒で反映）
```

- **AppSheet側**: 「Add row to 見積ジョブ」アクション＋自動化（任意で完了時にLINE/メール通知）。
- **GAS側（★実装済み 2026-07-07・`pm_jobs.js`）**: `runEstimateJobs()` 実装済み。毎分トリガーは `runAppJobs()`（見積ジョブ＋pmジョブを一括処理）に集約。既存の `createEstimateFromProcessSheet()`（東邦）/`createEstimateSheets()`（パイオニア）をそのまま再利用＝既存ロジック無変更。
  - セットアップ: GASエディタで `pmJobsSetupAll()` を1回実行（`見積ジョブ`/`pmジョブ`/`アプリ権限` シート作成＋毎分トリガー登録）。
  - シート列は §2-4 に `処理開始`/`処理完了` を運用列として追加（タイムアウト回収・監視用）。
  - 安全装置: クレームはロックで直列化／1回の実行で見積2件まで／処理中のまま30分超は自動でエラー回収／実行4.5分で残りは受付へ差し戻し。
- 代替案: AppSheet Automation の「Call a webhook」→ GAS Web App（要・共有トークン）。即時性は高いがエンドポイント管理が増える。**まずはジョブキュー方式を推奨**。

---

## 5. 権限（セキュリティ）

| 役割 | できること | AppSheet設定 |
|---|---|---|
| 管理者 | プロファイル/単価の編集・見積発行 | テーブル権限 Updates=ON |
| 一般 | 閲覧＋見積発行のみ（ルール編集不可） | プロファイル/単価は Read、見積ジョブは Add のみ |

- ユーザーは USEREMAIL() で判定。管理者メールを `管理者` 設定（小さなマスタシート or Security Filter）で制御。
- メインスプシ全体ではなく、AppSheetが見るのは上記テーブルのみ。

---

## 6. 構築手順（AppSheet側・移行後に実施）

1. AppSheetで「Start with your own data」→ メインスプシ（移行後）を選択。
2. テーブル `見積連携設定`/`見積単価設定`/`見積要確認`/`見積ジョブ` を追加。各キー列・型・参照を設定（§2）。
3. ビューを§3のとおり作成。プロファイル詳細に単価をインライン表示。
4. 「見積作成」アクション（Add row to 見積ジョブ）を作成し詳細ビューに配置。
5. 権限・USEREMAIL()ベースのSecurity Filterを設定。
6. （GAS）`runEstimateJobs()`＋毎分トリガーを追加（別途実装）。
7. テスト：1プロファイルで対象月を指定→ジョブ完了→結果URLを確認。

---

## 7. v1 確定仕様（おすすめ・非破壊で確定／2026-06-30）

1. **対象月の指定方法 → ドロップダウン（YYYY-MM）**。直近12か月を選択式（手入力の打ち間違い防止）。「当月／先月」をクイック選択で先頭に。`発行方式=完了行一括`（パイオニア）のプロファイルでは対象月欄は非表示（不要）。LINEの「5月分」発話は従来どおり併存＝既存を壊さない。
2. **完成通知 → ON（既存LINE資産を再利用）**。`runEstimateJobs()` が完了時に依頼者へLINEプッシュ＋結果URL（任意で社内グループにも）。既存 `sendLineMessage` を呼ぶだけ＝追加のみ・非破壊。失敗時はジョブの`メッセージ`にエラーを残し通知。
3. **管理者の範囲 → `アプリ権限`マスタシート（メール→役割）で制御**。コード埋め込みせず、シート編集で増減可能。AppSheetの Security Filter＋USEREMAIL() で判定。初期値は現運用の管理者メール。新規シートなので既存に影響なし。
4. **パイオニアの発行条件 → `発行方式`列で吸収（コード分岐にしない）**。
   - `月別`（東邦）: 対象月で工程を絞り `createEstimateFromProcessSheet()` を呼ぶ。
   - `完了行一括`（パイオニア）: 対象月は無視し「制作一覧の完了行」を `createEstimateSheets()` で一括発行。
   - 見積ジョブに `テストモード(先頭1店舗)` Yes/No を持たせ、ONなら `runEstimateTestOneStore()` 相当を呼ぶ。
   - → 既存の2つの生成関数を**そのまま再利用**。設定で切り替わるので新クライアントもコード追加不要。

> 反映済み: `見積連携設定` に `発行方式` 列を追加（estimate.js／名前参照・右端追加で非破壊。reader は `issueMode`、未設定＝`月別`）。
> 反映済み(2026-07-07): 上記1〜4のGAS側を `pm_jobs.js` に実装。
> - 対象月は `YYYY-MM`/`YYYY/M`/`YYYY年M月`/`当月`/`先月` を受理（`pmJobsParseTargetMonth`）。
> - 完成通知: 依頼者メールが `アプリ権限` シートにあり `LINEユーザーID` 記入済ならDM、無ければ社内グループ（INTERNAL_GROUP_ID）へ。
> - `アプリ権限` シート＝['メール','役割','表示名','LINEユーザーID','備考']。役割: 管理者/経理/一般。**行が0件の間は制限なし**（試運転用）、1行でも入れると登録メールのみ許可。金額承認は管理者/経理のみ。
> - `発行方式=完了行一括` は `テストモード` TRUEで `runEstimateTestOneStore()`（先頭1店舗）を呼ぶ。

### 7-2. 見積ジョブ 列の補足（§2-4 に追加）
| 列 | 型 | 用途 |
|---|---|---|
| テストモード | Yes/No | 完了行一括時に先頭1店舗だけ発行（試し） |

対象月は `発行方式=月別` のときのみ必須（AppSheetの Show/Required 条件で出し分け）。

関連: `アプリ化方針_AppSheet_2026-06-30.md` / `見積プロファイル_転記表.md` / `機能調査・リファクタ提案_2026-06-30.md`
