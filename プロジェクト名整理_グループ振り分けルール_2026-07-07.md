# プロジェクト名整理｜案件進捗とタスクの分離＋グループ振り分けルール（案）

作成: 2026-07-07 / 方針: 既存を壊さない・段階実施（差分提示→push→検証）。本書は設計レベル。
関連: `フェーズ1_整理プラン_案件ID統合.md` / `pm_groups.js`（案件グループ表） / `リファクタ_ドメイン規約.md`

---

## 0. 今回の整理の骨子（ご要望の整理）

1. **案件進捗管理**のプロジェクト名解決は、**名寄せ（文面からの推測一致）をやめ、グループ→案件の明示紐付けのみ**にする。
2. **社内グループは進捗管理の対象外**。1つの案件が複数グループにまたがるのは「案件グループ」表で束ねる。
3. **タスク・予定はグループ名そのまま**でよい（案件解決しない）。
   - どのグループで入ったタスク/予定かを**個人のDMに通知**、リマインドも個人DM。
   - タスクとして認識するか否かの見極めが難しいため、**DMで承認/拒否を選べる承認制**へ。
4. Botを**グループに招待したタイミングで「進捗管理する案件グループか否か」を振り分ける**ルールを作る。

→ つまり「プロジェクト名」は2種類に分離する：

| 用途 | 名前の正 | 解決方法 |
|---|---|---|
| 案件進捗（pm系統・フェーズ/金額/請求） | `projects.案件名`（案件ID） | **案件グループ表の明示紐付けのみ**。未紐付けなら動かない（推測しない） |
| タスク・予定・ファイル・メモ | **LINEグループ名そのまま** | 解決不要。グループ表示名をそのまま記録 |

---

## 1. 現状の問題（コード根拠）

| 問題 | 現状の実装 |
|---|---|
| join時に必ず「どの案件？」を聞く | `handleBotJoinGroup()`（コード.js:2038）が全グループに案件選択UIを出す。社内グループでも案件紐付けを迫られる |
| タスクの案件名を推測で当てる | `resolveProjectForAutoCommit()`（コード.js:340）が未紐付けグループで `getProjectCandidates()` confidence≥70 の名寄せ → 誤マッチの温床。外れると「未分類」 |
| 即登録・見極め不能 | `detectTasksSilently()` → `commitDirectly()`（コード.js:1241）が確認なしで本登録＋DM通知＋カレンダー反映 |
| 1案件複数グループ | 辞書の施主/業者2列が上限。→ 案件グループ表（pm_groups.js）で解消済みだが、join経路がまだ旧2列（`linkGroupToProject()` は col3 に書くだけ） |
| 社内グループの区別がない | 種別「社内」は案件グループ表に定義済みだが、振り分ける入口がない |

---

## 2. グループ振り分けルール（案）

### 2.1 join時の振り分けフロー

Botをグループに招待した瞬間に**グループ種別を確定**させる。案件選択より先に種別を聞く。

```
Bot参加（join）
 ├─ ① グループ表示名を LINE API（GET /group/{id}/summary）で取得・記録
 ├─ ② 自動判定ヒントを添えて Quick Reply を表示：
 │     「このグループの用途を選んでください」
 │      [🏗 案件グループ（進捗管理する）]
 │      [🏢 社内・連絡用（タスクのみ）]
 │      [⏸ あとで設定]
 ├─ ③-a 案件グループ → 既存案件リスト（getActiveProjects）＋「🆕新規案件」
 │        → 選択で 案件グループ表 に（グループID, 案件ID, 種別=施主/業者/元請…, 表示名）を登録
 ├─ ③-b 社内・連絡用 → 案件グループ表 に（グループID, 案件ID=空, 種別=社内, 表示名）を登録
 └─ ③-c 未設定 → 何も登録しない（下記 2.3 の既定動作）
```

### 2.2 自動判定の補助ルール（最終確定は人のタップ）

②の選択UIに「おすすめ」を付ける判定。**自動確定はしない**（誤爆防止）。

| 優先 | 条件 | おすすめ |
|---|---|---|
| 1 | グループ名に「社内」「事務」「経理」「WOODBASE」「共有」「連絡」を含む | 社内・連絡用 |
| 2 | グループ名が既存案件名（projects.案件名 / 辞書の略称・正式名称）と部分一致 | その案件の案件グループ（候補を先頭に表示） |
| 3 | 上記いずれも該当なし | 案件グループを既定選択（新規案件の可能性が高い） |

※ ここでの案件名一致は「join時のUI候補の並べ替え」にだけ使う。**運用中のメッセージ処理では一切名寄せしない**。

### 2.3 種別ごとの動作マトリクス

| 動作 | 案件グループ | 社内・連絡用 | 未設定 |
|---|---|---|---|
| pm系統（案件更新/請求/入金 等） | ✅ 紐付け案件で動く | ❌「このグループは進捗管理対象外です」 | ❌「案件未紐付けです。@WBG 設定 で紐付けできます」 |
| タスク・予定の自動検出 | ✅ project=**グループ名** | ✅ project=**グループ名** | ✅ project=**グループ名** |
| タスクのDM承認 | ✅ | ✅ | ✅ |
| ファイル保存（Drive） | ✅ 案件フォルダ | ✅ グループ名フォルダ | ✅ グループ名フォルダ |
| 毎朝ダイジェスト等の案件集計 | ✅ | 対象外 | 対象外 |

- タスク・予定は**全種別でグループ名そのまま**。案件グループであっても、タスクの project_name はグループ名でよい（案件進捗と混ぜない）。案件との対応が見たい場合は案件グループ表を介せば辿れる。
- 後から種別を変えたい場合用に、グループ内コマンド `@WBG 設定` で振り分けUIを再表示できるようにする。

---

## 3. タスク・予定のDM承認制（案）

現行の `detectTasksSilently → commitDirectly（即登録）` を次に変更：

```
グループ通常メッセージ
 → Gemini抽出（現行のまま）
 → 仮タスク保存（既存「仮タスク」シートを再利用）
 → 承認先のDMへ Quick Reply：
    「📥 [グループ名] でタスク候補を検出しました
      内容: ○○ / 期日: ○○ / 担当: ○○
      [✅ 登録する] [✏️ 修正する] [🗑 登録しない]」
 → ✅ 承認 → 本登録（writeTask/writeSchedule）＋カレンダー反映＋関係者DM通知（現行の commitDirectly 相当）
 → 🗑 拒否 → 仮タスク削除（学習用に「拒否ログ」へ1行残す：将来の見極め精度向上の材料）
```

- **承認先**: 担当者（assignee）が特定できれば担当者のDM。特定できなければ送信者本人のDM。@メンションされた人がいれば同報。
- **リマインド**: 承認済みタスクのリマインドは現行どおり個人DM。**承認待ちのまま24時間経過**したら承認先へ再打診を1回だけ送る。
- **無応答時の既定**: 要決定（§5）。案は「48時間で自動破棄（拒否ログへ）」。現行の即登録に慣れているなら「自動確定」も選べる。
- グループには一切返信しない（現行の「水面下」原則は維持）。
- 承認/拒否のUIは pm_pending.js（金額承認のDM承認制）と同じ postback パターンで実装できる。

---

## 4. 実装ステップ（非破壊・段階実施）

| 順 | 内容 | 破壊性 | 実装先 |
|---|---|---|---|
| 1 | グループ表示名取得 util（LINE group summary API）＋案件グループ表の「表示名」自動記入 | なし | 新規 util（pm_groups.js か コード.js） |
| 2 | `handleBotJoinGroup()` を種別選択ファースト（§2.1）に変更。登録先を旧2列から**案件グループ表**へ切替（`linkGroupToProject` は残すが呼ばない） | 低 | コード.js:2038 / postback |
| 3 | `resolveProjectForAutoCommit()` から名寄せ（confidence≥70）と「未分類」を撤去 → **常にグループ名を返す**（DMはグループIDが無いので「DM」等） | 低 | コード.js:340 |
| 4 | DM承認制（§3）: `commitDirectly` の手前に仮タスク＋DM承認を挿入。承認postback→本登録 | 中 | コード.js（既存仮タスク機構を再利用） |
| 5 | pm系統: `pmResolveProject()` の identifyProject 名寄せフォールバックを外し、**グループ紐付け＋明示案件名指定のみ**に。社内種別は対象外応答 | 中 | pm_manager.js:115 |
| 6 | 既存グループの棚卸し: `backfillProjectGroups()` 実行＋メッセージログ上の既知グループ一覧に種別未設定を出力 → 手動で種別付け | なし | 既存＋レポート関数 |
| 7 | 旧経路の凍結: 施主/業者2列は読み取りフォールバックのみ（getProjectNameByGroupId は現状のまま非破壊） | なし | 変更なし |

ステップ2〜3が「振り分け＋グループ名そのまま」の本体。4が承認制。5は案件進捗の名寄せ廃止。
※ 名寄せ関数群（identifyProject 等）は**削除しない**。住所照会・進捗質問など他機能が使うため、「タスク登録と pm 更新の経路から外す」だけ。

---

## 5. 要決定事項

| # | 論点 | 案A（おすすめ） | 案B |
|---|---|---|---|
| 1 | 承認待ちの無応答時 | 48時間で自動破棄（拒否ログへ） | 24時間で自動確定（現行の即登録に近い） |
| 2 | 承認先 | 担当者DM（不明なら送信者DM） | 常に送信者DM |
| 3 | 予定（カレンダー反映）も承認制にするか | 予定も承認制（誤カレンダー登録防止） | 予定は即登録のまま（タスクのみ承認制） |
| 4 | 社内グループの予定のカレンダー反映 | する（タスク系は種別に依らず同じ） | しない |
