# プロジェクト進捗管理機能 設計書（追加機能 / MVP）

作成日: 2026-06-18
対象: WOODBASE LINE秘書AI への「プロジェクト進捗管理・放置アラート・請求漏れ防止・カレンダー連携・Drive連携」追加
方針: 既存のLINEタスク管理を壊さず、**新規モジュールとして分離**して追加する

---

## 0. 結論（先に要点）

既存システムには、今回ほしい機能の**土台がほぼ揃っている**。新規開発ではなく「接続」で実現できる。

| 今回ほしいもの | 既存で流用できるもの |
|---|---|
| LINEから報告を受ける | `doPost` → `handleEvent` → `handleDM`/`handleGroup` |
| AIで内容抽出 | `callGemini()`（Gemini 2.5 Flash）, `extractWithGemini()` |
| 案件名の特定・誤名寄せ防止 | `identifyProject()`（信頼度付き）＋ 案件候補Quick Reply UI（`set_project`/`new_project` postback） |
| グループ↔案件の紐付け | `getProjectNameByGroupId()`, `linkGroupToProject()`, プロジェクト管理シート |
| タスク自動作成 | `writeTask()`（タスク管理シート） |
| カレンダー登録 | `addToCalendar()`, `addTaskToCalendar()`, `hasSimilarCalendarEvent()` |
| Drive保存 | `saveFileToDrive()`, `DRIVE_FOLDER_ID` |
| 毎朝チェック | `createTriggers()`, `checkDeadlines()`（毎日9:00） |
| シート操作 | `getSS()`, `getSheet()`, `setupSheet()` |
| LINE送信 | `sendLineReply()`, `sendQuickReply()`, `sendLineMessage()` |

→ 追加するのは「プロジェクト進捗の**書き込み**ロジック」と「請求/金額の**確認待ち**ワークフロー」「放置/請求アラート」が中心。

---

## 1. 既存システムの構成確認

### 1.1 ファイル構成
| ファイル | 役割 |
|---|---|
| `コード.js` (3874行) | Webhook受信・ルーティング・タスク/予定・カレンダー・トリガー・共通処理 |
| `progress.js` (688行) | **外部**進捗管理表への自然文クエリ（読み取り専用）。今回の機能とは別物 |
| `summary.js` (635行) | 案件サマリーをGoogle Docsへ自動生成 |
| `estimate.js` / `estimate_pioneer.js` | 工程管理表→見積書の自動生成 |
| `migrate.js` | データ移行補助 |

### 1.2 重要な既存関数（接続先・流用元）
| 関数 | 行 | 用途 |
|---|---|---|
| `doPost(e)` | 88 | Webhook入口 |
| `handleEvent(ev)` | 114 | イベント振り分け |
| `handleDM(ev)` | 188 | 1対1トーク処理（コマンド連鎖） |
| `handleGroup(ev)` / `handleMentionCommand` | 236 / 259 | グループ処理・メンションコマンド |
| `handlePostback(ev)` | 960 | ボタン操作の振り分け |
| `extractWithGemini()` | 533 | テキスト→tasks/schedules抽出 |
| `identifyProject()` | 618 | 案件名特定（信頼度0-100） |
| `callGemini()` | 2747 | Gemini呼び出し |
| `writeTask()` | 1483 | タスク管理シートへ追記 |
| `addToCalendar()` / `addTaskToCalendar()` | 1514 / 1557 | カレンダー登録 |
| `hasSimilarCalendarEvent()` | 1592 | カレンダー重複防止 |
| `getProjectNameByGroupId()` / `linkGroupToProject()` | - | グループ↔案件 |
| `createTriggers()` | 3209 | トリガー一括登録 |
| `getConfig()` / `getSheet()` / `getSS()` / `setupSheet()` | 10 / 24 / 23 / 3173 | 設定・シート |
| `sendLineReply()` / `sendQuickReply()` / `sendLineMessage()` | 3025 / 890 / 3037 | LINE送信 |

### 1.3 既存の設計思想（今回も踏襲すべき点）
- **二段確認**: 抽出→「仮」保存→ボタン確認→本登録（誤登録防止）
- **誤名寄せ防止**: AIで案件確定せず、**候補提示→人間が選択**
- **非破壊**: `setup()` は既存データを消さず、不足列は右端へ追加
- **状態管理**: 短期状態は `PropertiesService` にTTL付きで保存（`RETRY_<userId>` など）
- **過剰反応抑制**: グループ通常会話は無反応（ログのみ）

---

## 2. 既存に影響を出さない追加方針

1. **新規ファイルに分離**（後述のモジュール一覧）。`コード.js` への追記は「接続用の1〜数行」のみ。
2. **グローバル名前空間の衝突回避**: 新規トップレベル関数は全て `pm`（project management）プレフィックスを付ける（例 `pmRouteText`）。GASは全`.js`が同一グローバル名前空間のため必須。
3. **新規シートは英語名**（projects, project_logs…）。既存の日本語シート名と衝突しない。
4. **発火を明示キーワードに限定（MVP）**: `案件更新` `進捗更新` `新規案件` `請求更新` `入金更新` などの**文頭プレフィックス**でのみ起動。既存のタスク抽出フローには一切割り込まない。
5. **既存タスク管理シートへの変更は「列追加のみ」**（`関連案件ID`, `作成元`）。既存の非破壊方針（不足列は右端追加）に準拠。
6. **トリガーは独立**: 既存 `checkDeadlines`（9:00）に相乗りせず、`pmCheckAlerts` を別関数として 9:05 などに登録（障害切り分け容易）。
7. **金額・請求・入金は自動確定しない**: AIは候補抽出のみ。`project_pending_updates` に積み、人間承認で確定。

---

## 3. 追加するファイル・モジュール一覧

| 新規ファイル | モジュール | 主な責務 | 主要関数（全て `pm` 接頭辞） |
|---|---|---|---|
| `pm_router.js` | ルーティング | LINE報告の入口判定・postback振り分け | `pmRouteText()`, `pmRoutePostback()` |
| `pm_parser.js` | ProjectParser | Geminiでintent＋項目抽出（厳格JSON） | `pmParseReport()`, `pmClassifyIntent()` |
| `pm_manager.js` | ProjectManager | projects/project_logsの読み書き・案件解決 | `pmResolveProject()`, `pmCreateProject()`, `pmApplyUpdate()`, `pmGetProjectsSheet()` |
| `pm_pending.js` | 確認待ち | 確認待ち（金額/請求/案件曖昧）の登録・承認 | `pmQueuePending()`, `pmApprovePending()`, `pmListPending()` |
| `pm_alert.js` | ProjectAlert | 放置/請求漏れ/入金未確認の検知・通知 | `pmCheckAlerts()`, `pmCheckStale()`, `pmCheckBilling()` |
| `pm_calendar.js` | ProjectCalendar | 日付項目→カレンダー（イベントID保持で重複防止） | `pmSyncCalendar()` |
| `pm_drive.js` | ProjectDrive | 案件フォルダ自動作成・URL保存 | `pmEnsureProjectFolder()` |
| `pm_taskbridge.js` | ProjectTaskBridge | 次回アクション→既存タスク自動作成（重複防止） | `pmCreateFollowupTask()` |
| `pm_setup.js` | セットアップ | 新規シート作成・トリガー登録・テスト | `pmSetupSheets()`, `pmSetupTriggers()`, `pmTest*()` |

### 既存ファイルへの接続（最小差分・3〜4箇所）
```text
コード.js handleDM() / handleMentionCommand() 内、既存コマンド判定の前後に:
    if (pmRouteText(ev, text, isGroup)) return;   // ← 1行追加

コード.js handlePostback() の冒頭付近に:
    if (pmRoutePostback(ev, data)) return;        // ← 1行追加

コード.js createTriggers() の末尾に:
    pmSetupTriggers();                            // ← 1行追加（または個別実行）

コード.js setup() の末尾に（任意）:
    pmSetupSheets();                              // ← 1行追加（または個別実行）
```
※ `pmRouteText` は明示キーワードに当たらなければ即 `false` を返し、既存処理に影響を与えない。

---

## 4. 追加するスプレッドシート（シート・列定義）

すべて**メインスプレッドシート**（`SPREADSHEET_ID`）内に追加。1行目をヘッダーとする（既存慣習）。

### 4.1 `projects`（プロジェクト管理表＝正）
| 列 | 説明 | 自動/確認 |
|---|---|---|
| 案件ID | `2026-001` 形式（年-連番）。主キー | 自動 |
| 案件名 | 正式名称 | 自動 |
| クライアント名 | | 自動 |
| 担当者 | | 自動 |
| LINEグループID | 発生元グループ | 自動 |
| DriveフォルダURL | pm_driveが格納 | 自動 |
| 現在フェーズ | 営業/設計・デザイン/施工準備/施工/お引き渡し | 自動 |
| 営業ステータス | §7参照 | 自動 |
| 設計・デザインステータス | | 自動 |
| 施工準備ステータス | | 自動 |
| 施工ステータス | | 自動 |
| お引き渡しステータス | | 自動 |
| 最終更新日 | 報告のたび更新 | 自動 |
| 次回アクション | | 自動 |
| 次回アクション期限 | | 自動 |
| 打合せ日 | | 自動 |
| 着工予定日 | | 自動 |
| 引き渡し予定日 | | 自動 |
| 売上 | | **確認** |
| 原価 | | **確認** |
| 利益額 | | **確認** |
| 利益率 | | **確認** |
| 請求予定日 | | 自動（日付のみ）/金額は確認 |
| 請求済日 | | **確認** |
| 入金予定日 | | 自動（日付のみ）/金額は確認 |
| 入金確認日 | | **確認** |
| 請求ステータス | 未請求/一部請求/請求済 | **確認** |
| 入金ステータス | 未入金/一部入金/入金済 | **確認** |
| 備考 | | 自動 |
| 更新元メッセージ | 直近報告の原文 | 自動 |
| 最終更新者 | LINE発信者名 | 自動 |
| 作成日 | | 自動 |
| 更新日 | | 自動 |

### 4.2 `project_logs`（更新履歴）
| 列 | 説明 |
|---|---|
| ログID | 連番 |
| 日時 | |
| 案件ID | |
| intent | project_update / billing_update など |
| 変更項目 | JSON（フィールド→旧値/新値） |
| 更新元メッセージ | 原文 |
| 更新者 | LINE発信者 |
| グループID | |
| 適用区分 | auto / approved |

### 4.3 `project_alerts`（アラート履歴・重複防止）
| 列 | 説明 |
|---|---|
| アラートID | 連番 |
| 案件ID | |
| 種別 | stale / billing_overdue / payment_overdue |
| アラートキー | `案件ID:種別:対象日`（重複送信判定キー） |
| 検知日時 | |
| 通知先 | 担当者/グループ |
| 内容 | 送信本文 |
| 状態 | sent |

### 4.4 `project_calendar_events`（カレンダー重複防止）
| 列 | 説明 |
|---|---|
| 案件ID | |
| イベント種別 | next_action / meeting / construction_start / handover / billing_due / payment_due |
| イベントID | Googleカレンダーのevent id |
| 開始日時 | |
| 終了日時 | |
| 担当者 | |
| 作成日 | |
| 更新日 | |

→ `案件ID × イベント種別` が既存なら**新規作成せず更新**（idempotent）。

### 4.5 `project_pending_updates`（人間確認が必要な更新候補）
| 列 | 説明 |
|---|---|
| 確認ID | 連番（postbackで参照） |
| 作成日時 | |
| 案件ID | 確定済みなら格納。曖昧なら空 |
| 案件名候補 | 曖昧時の候補（JSON配列） |
| type | project_field / billing / amount / project_create |
| intent | |
| 抽出内容 | JSON（フィールド→候補値） |
| 更新元メッセージ | 原文 |
| 申請者 | LINE発信者 |
| グループID | |
| status | awaiting_project / awaiting_approval / applied / rejected |
| 承認者 | |
| 処理日時 | |

### 4.6 既存 `タスク管理` への列追加（非破壊）
- `関連案件ID`（pmが作成したタスクに案件IDを記録）
- `作成元`（`project_update` など。空＝従来の手動/AI抽出タスク）

---

## 5. LINE報告パターン一覧（MVP：ゆるいルール）

文頭プレフィックスで起動。プレフィックスが無ければ従来どおり（pmは介入しない）。

| プレフィックス | intent | 例 | 抽出主項目 |
|---|---|---|---|
| `案件更新` / `進捗更新` | project_update | 「案件更新 〇〇ホテル、見積提出しました。6/25にフォローします。」 | 案件名・ステータス・次回アクション・期限 |
| `新規案件` | project_create | 「新規案件 案件名:〇〇ホテル客室改装 クライアント:ABC運営 担当:濱田 売上予定:5000万」 | 案件名・クライアント・担当・(金額は確認待ち) |
| `請求更新` / `請求` | billing_update | 「請求更新 〇〇ホテル、中間金を7/1に請求予定です。」 | 案件名・請求予定日・請求種別（**確認待ち**） |
| `入金更新` / `入金` | payment_update | 「入金更新 〇〇ホテル、7/10入金予定。」 | 案件名・入金予定日（**確認待ち**） |
| `予定追加`（任意） | calendar_update | 「予定追加 〇〇ホテル 7/3 14時 現地打合せ」 | 案件名・日時・種別 |

補足:
- グループが案件紐付け済みなら案件名省略可（`getProjectNameByGroupId`で解決）。
- プレフィックスは半角/全角・コロン有無を吸収（既存プレフィックス処理に倣う）。
- 将来的には「プレフィックスなし自由文」へ拡張可能（intent判定の閾値を上げる）。MVPでは精度優先で明示キーワード。

---

## 6. AI抽出JSONスキーマ（ProjectParser）

`callGemini()` に厳格JSONを要求。`pmParseReport()` が返す型:

```json
{
  "intent": "project_create | project_update | billing_update | payment_update | calendar_update | unknown",
  "project_name": "string|null",
  "project_id": "string|null",
  "client_name": "string|null",
  "assignee": "string|null",
  "phase": "営業|設計・デザイン|施工準備|施工|お引き渡し|null",
  "status": "string|null",
  "next_action": "string|null",
  "next_action_due_date": "YYYY-MM-DD|null",
  "meeting_date": "YYYY-MM-DD|null",
  "construction_start_date": "YYYY-MM-DD|null",
  "handover_date": "YYYY-MM-DD|null",
  "billing_due_date": "YYYY-MM-DD|null",
  "billing_done_date": "YYYY-MM-DD|null",
  "payment_due_date": "YYYY-MM-DD|null",
  "payment_confirmed_date": "YYYY-MM-DD|null",
  "amount": { "type": "売上|原価|請求|入金|null", "value": "number|null", "raw": "string|null" },
  "remark": "string|null",
  "confidence": 0,
  "needs_confirmation": false,
  "confirmation_reason": "string|null"
}
```

**needs_confirmation = true にする条件（強制）**
- `intent` が billing_update / payment_update（請求・入金は常に確認）
- `amount.value` が非null（金額が絡む）
- `confidence < 70`
- 案件名候補が複数（曖昧）

**日付の正規化**: 「6/25」「来週月曜」等は `pmParser` 側で当日（`currentDate`基準, Asia/Tokyo）を起点に `YYYY-MM-DD` へ解決。年が無い場合は直近未来日に補完。

---

## 7. フェーズ・ステータス定義（バリデーション用マスタ）

`pm_manager.js` に定数として保持し、AI抽出値をこの集合へマッピング（最近傍/別名吸収）。集合外は `備考` に残し `needs_confirmation`。

```
営業: 引合／問合せ受付 / 現地調査／ヒアリング / 概算見積済 / 見積提出（フォロー中） / 契約書確認中 / 受注 / 保留（長期検討） / 失注
設計・デザイン: 未着手 / 基本設計中 / 実施設計中 / お客様確認待ち / 修正対応中 / 承認済
施工準備: 未着手 / 工程表作成中 / 工程表完成 / 発注／材料手配中 / 職人手配中 / 近隣挨拶 / 原価確定（着工前） / 確認申請中
施工: 未着手 / 着工 / 施工中（進捗％） / 中間検査 / 行政検査 / 社内検査 / 是正期間
お引き渡し: 未 / 完了検査 / 引き渡し準備 / 引き渡し済 / 最終請求済 / 入金確認 / アフター／保証期間
```

---

## 8. AI自動更新 / 確認必須 の振り分け

| 区分 | 項目 | 反映先 |
|---|---|---|
| **自動更新OK** | フェーズ / 各ステータス / 最終更新日 / 次回アクション / 次回アクション期限 / 担当者 / 備考 / カレンダー予定候補 | `pmApplyUpdate()` が即 projects へ反映＋project_logsへ記録 |
| **確認必須** | 売上 / 原価 / 利益額 / 利益率 / 請求金額 / 入金金額 / 請求済確定 / 入金確認済確定 | `pmQueuePending()` → project_pending_updates（status=awaiting_approval） |

確認フロー:
```
AI抽出（金額/請求/入金あり） → project_pending_updates へ保存
  → 管理/経理グループへ「確認待ち1件」通知（Quick Reply: 承認 / 却下 / 内容表示）
  → postback pm_approve / pm_reject
  → 承認時 pmApprovePending() が projects へ確定反映＋project_logs（適用区分=approved）
```

---

## 9. アラート設計（毎朝トリガー）

`pmCheckAlerts()` を毎日 9:05 に実行（既存 `checkDeadlines` とは独立）。重複送信は `project_alerts` のアラートキーで防止。

| アラート | 条件 | 通知先 | 文面例 |
|---|---|---|---|
| 放置 | フェーズ=見積提出（フォロー中）かつ最終更新日から**7日**経過 / または次回アクション期限超過 / または最終更新日から**N日**無更新 | 担当者DM＋必要時 管理グループ | 「【放置アラート】〇〇ホテル客室改装が見積提出後7日間更新されていません。担当:濱田 / 次回:6/25」 |
| 請求漏れ | 請求予定日 < 今日 かつ 請求済日が空 かつ 請求ステータス=未請求 | 担当者＋経理 | 「【請求漏れアラート】〇〇ホテルの請求予定日(7/1)を過ぎています。状態:未請求」 |
| 入金未確認 | 入金予定日 < 今日 かつ 入金確認日が空 かつ 入金ステータス=未入金 | 担当者＋経理 | 「【入金未確認アラート】〇〇ホテルの入金予定日(7/10)を過ぎています。状態:未入金」 |

しきい値（7日, N日）は `settings` か `pm_alert.js` の定数で調整可能にする。

---

## 10. Googleカレンダー連携設計（ProjectCalendar）

対象日付: 次回アクション期限 / 打合せ日 / 着工予定日 / 引き渡し予定日 / 請求予定日 / 入金予定日

```
pmSyncCalendar(projectId):
  for 各日付項目:
    既存 project_calendar_events に (案件ID, 種別) あり？
      ├ あり: 日付が変わっていれば event.setTime/ setAllDayDate で更新、行を更新
      └ なし: addToCalendar相当で作成 → eventId を project_calendar_events に保存
```
- 既存 `hasSimilarCalendarEvent()` をフォールバックの重複チェックに併用。
- `CALENDAR_ID`（既存設定）を使用。
- 担当者のGoogleメール招待は既存 `getMemberGoogleEmailsFromNames()` を流用可。
- MVPでは「次回アクション期限・打合せ日」のみ自動、他は後続フェーズで有効化（段階導入）。

---

## 11. Google Drive連携設計（ProjectDrive）

新規案件登録時に `pmEnsureProjectFolder(projectId, projectName)`:
```
共有ドライブ/プロジェクト管理/   （DRIVE_FOLDER_ID 配下に「プロジェクト管理」フォルダを担保）
  └ {案件ID}_{案件名}/
       ├ 01_見積  ├ 02_図面  ├ 03_契約書
       ├ 04_写真  ├ 05_請求書 └ 06_議事録
```
- 既存 `DRIVE_FOLDER_ID` を親に使用。サブフォルダ構成は定数配列で定義。
- **冪等**: projects の DriveフォルダURL が既にあれば作成しない。
- 作成後、URLを projects の `DriveフォルダURL` に保存。
- 既存のファイル自動保存(`saveFileToDrive`)を、将来この案件フォルダへ振り分けるよう拡張可能（MVPでは未分類のまま、フォルダ作成のみ）。

---

## 12. 既存タスク管理との連携（ProjectTaskBridge）

`project_update` で `next_action` ＋ `next_action_due_date` が取れたら、既存 `writeTask()` でタスク自動作成。
```
pmCreateFollowupTask(projectId):
  タスク名 = "{案件名}の{next_action}"   例: 〇〇ホテルの見積フォロー
  担当者   = 案件担当者 or LINE発信者
  期日     = next_action_due_date
  関連案件ID = projectId
  作成元   = "project_update"
  重複防止: 同一(関連案件ID, タスク内容, 期日)が未完了で存在 → 作成しない
```
- 既存タスクのライフサイクル（担当者通知・期日リマインド）にそのまま乗る。

---

## 13. 案件名の解決と曖昧時フロー（誤名寄せ防止）

`pmResolveProject(text, groupId)`:
```
1. groupId が紐付け済み → 即確定（既存 getProjectNameByGroupId）
2. 既存 identifyProject() で候補＋信頼度
3. confidence >= 90 かつ 単一候補 → 確定
4. それ以外 → project_pending_updates(status=awaiting_project) に保存し、
   候補(最大4)＋「該当なし/新規」を Quick Reply 提示
       postback pm_pick_project:{確認ID}:{案件ID}
5. 新規選択時は既存 new_project フロー（finalizeNewProject）に合流 or pmCreateProject()
```
例:「〇〇ホテルに該当する案件が複数あります。更新する案件を選んでください ①2026-001 客室改装 ②2026-014 宴会場改装」

---

## 14. MVP実装ステップ

| 順 | 内容 | 成果物 |
|---|---|---|
| 1 | `pm_setup.js`：projects / project_logs シート作成、タスク管理へ列追加 | `pmSetupSheets()` |
| 2 | `pm_manager.js`：projects CRUD・案件ID採番・案件解決・ステータスマッピング | `pmResolveProject`, `pmCreateProject`, `pmApplyUpdate` |
| 3 | `pm_parser.js`：intent判定＋JSON抽出（callGemini流用） | `pmParseReport` |
| 4 | `pm_router.js`：明示キーワード入口＋postback振り分け（コード.jsへ1行接続） | `pmRouteText`, `pmRoutePostback` |
| 5 | 新規案件登録（project_create、金額は確認待ち） | 動作確認 |
| 6 | 案件進捗更新（project_update、最終更新日自動・次回アクション抽出） | 動作確認 |
| 7 | `pm_taskbridge.js`：次回アクション→既存タスク自動作成（重複防止） | `pmCreateFollowupTask` |
| 8 | `pm_alert.js`：放置アラート＋毎朝トリガー登録 | `pmCheckAlerts`, `pmSetupTriggers` |
| 9 | `pm_pending.js`：請求予定日/入金予定日の候補抽出＆確認待ち保存＋承認postback | `pmQueuePending`, `pmApprovePending` |
| 10 | 請求漏れ/入金未確認アラート | `pmCheckBilling` |

### MVPで後回し
- カレンダー全項目連携（`pm_calendar.js` は次回アクション/打合せのみ先行）
- Drive自動作成の本格運用（フォルダ作成までは先行可、ファイル振り分けは後）
- 金額の自動確定（常に確認待ち）
- 全LINEグループからの完全自動抽出（プレフィックス運用で開始）
- 外部業者向け画面 / 独自アプリ化

---

## 15. テスト項目

### 単体（GASエディタから実行）
- `pmTestParse_projectUpdate()`：「案件更新 〇〇ホテル、見積提出。6/25フォロー」→ intent/ステータス/期限が正しいJSON
- `pmTestParse_billing()`：請求文 → needs_confirmation=true
- `pmTestResolveProject_ambiguous()`：類似案件2件 → 候補提示（自動確定しない）
- `pmTestApplyUpdate_safeFields()`：自動更新項目のみ反映、金額は触らない
- `pmTestStatusMapping()`：表記ゆれ → 正規ステータスへ
- `pmTestSetupSheets_nonDestructive()`：再実行で既存データを消さない／重複列を作らない
- `pmTestTaskBridge_dedup()`：同一フォロータスクの二重作成なし
- `pmTestCalendarIdempotent()`：同案件・同種別で重複イベントを作らない（更新になる）
- `pmTestDriveIdempotent()`：URL既存なら再作成しない

### 結合
- LINE報告（project_create）→ projects追記＋Driveフォルダ＋（金額は確認待ち）
- LINE報告（project_update）→ projects更新＋最終更新日＋フォロータスク作成
- 請求更新 → 確認待ち→承認 → projects確定＋project_logs(approved)
- 毎朝 `pmCheckAlerts` → 放置/請求漏れ/入金未確認の通知＋重複送信なし

### 受け入れ基準
- 既存タスク登録・進捗質問・見積・サマリーが**従来どおり動作**（回帰なし）
- 金額・請求・入金が**人間承認なしに確定しない**
- 案件名が曖昧なとき**勝手に更新しない**
- アラートが同一案件・同一条件で**1日1回まで**

---

## 16. 主要な懸念点への対策（会議論点との対応）

| 会議の懸念 | 対策 |
|---|---|
| 3-1 新アプリは浸透しない | 既存LINE Bot内で完結。新アプリ・新画面なし |
| 3-2 どこまで自動で拾えるか | MVPは**明示プレフィックス**で起動。全自動は段階導入 |
| 3-3 案件名の紐付けミス | `identifyProject`＋候補提示UI（自動確定しない） |
| 3-4 金額の自動更新は危険 | 金額/請求/入金は `project_pending_updates`→人間承認 |
| 3-5 進捗のブラックボックス化 | 最終更新日/次回アクション/担当者/フェーズを管理＋放置アラート |
| 3-6 請求漏れ・入金漏れ | 日付項目を分離（予定日/済日）＋アラート |
| 3-7 TimeTree vs Google | Googleカレンダー前提（GAS親和・既存連携あり） |
| 3-8 作り込みすぎ | MVPで小さく開始→運用しながら拡張する構成 |

---

## 17. 確定事項（2026-06-18 ユーザー回答）
1. データ配置：**メインスプレッドシート内に追加**（`SPREADSHEET_ID`）。
2. 金額・請求の確認待ち通知先：**経理専用グループを新設** → Script Property `ACCOUNTING_GROUP_ID` を設定（未設定時は `INTERNAL_GROUP_ID` にフォールバック）。
3. 放置アラートしきい値：**見積提出後7日／無更新14日**（`PM_STALE_AFTER_QUOTE_DAYS=7`, `PM_STALE_NO_UPDATE_DAYS=14`）。
4. MVP：**全体を一気に実装**（実装完了。下記参照）。
5. Drive：既存 `DRIVE_FOLDER_ID` 直下に「プロジェクト管理」フォルダを担保し、その配下に案件フォルダを作成。

## 18. 実装状況（MVP 実装済み）

追加ファイル：`pm_core.js` / `pm_manager.js` / `pm_parser.js` / `pm_pending.js` / `pm_taskbridge.js` / `pm_calendar.js` / `pm_drive.js` / `pm_alert.js` / `pm_router.js` / `pm_tests.js`

既存 `コード.js` への接続（4箇所のみ・非破壊）：
- `handleDM()`：進捗クエリ判定の直後に `pmRouteText(...)` を追加
- `handleGroup()`：プレフィックス処理直後・メンション判定前に `pmRouteText(...)` を追加（メンション不要で報告可能）
- `handlePostback()`：冒頭で `pmRoutePostback(...)` を最優先処理
- `setup()` 末尾で `pmSetupSheets()`、`createTriggers()` 末尾で `pmSetupTriggers()` を呼び出し

### 導入手順（運用者）
1. clasp push でコードを反映（編集アカウント woodbasegroup@gmail.com）
2. GASエディタで `setup()` を再実行（projects等のシートが非破壊で追加される）
3. Script Property に `ACCOUNTING_GROUP_ID`（経理グループID）を設定
4. `createTriggers()` を再実行（毎朝9時 `pmCheckAlerts` が登録される）
5. `pmDiagnose()` で接続確認 → `pmTestClassifyIntent()` 等でテスト
6. LINEで「案件更新 〇〇、見積提出。6/25フォロー」を送って動作確認
```

