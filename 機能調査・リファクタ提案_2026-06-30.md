# GAS LINE秘書 機能調査・整理・リファクタ提案

調査日: 2026-06-30 / 対象: WOODBASE LINE秘書AI（clasp pull済みGASソース一式）
方針: 本書は**設計レベルの調査・提案のみ**。実装コードは合意後に別途。

---

## Step 1: コード全体の棚卸し

### 1.1 ファイル一覧と役割（1行要約）

| ファイル | 行数 | 役割 |
|---|---|---|
| `コード.js` | 4013 | **モノリス本体**。Webhook受信(`doPost`)・イベント振り分け・既存タスク/予定抽出・カレンダー・メンバー管理・トリガー・共通処理(`getSS`/`getSheet`/`callGemini`/`sendLineReply`) |
| `estimate.js` | 1044 | **東邦家具系見積**。工程管理表→月別見積書。**設定シート対応**（見積連携設定/見積単価設定/見積要確認） |
| `estimate_pioneer.js` | 753 | **パイオニア什器系見積**。制作一覧→店舗別見積書。estimate.jsと**完全独立**・設定は`EST_PIONEER`に**ハードコード** |
| `progress.js` | 688 | **【A系統】外部「進捗管理表」への自然文クエリ（読取専用）**。「図面チェックできてない店舗ある？」等に回答＋毎朝ダイジェスト |
| `summary.js` | 635 | 案件サマリーをGoogle Docsへ自動生成（Gemini要約・差分検出・毎朝バッチ） |
| `pm_core.js` | 249 | **【B系統】基盤**。シート名定数・`PM_PHASES`(5フェーズマスタ)・`pmProp`/`pmWithLock`/シート読み書きヘルパー・`pmSetupSheets`/`pmSetupTriggers` |
| `pm_manager.js` | 238 | 【B系統】`projects`のCRUD・案件ID採番・案件解決・ステータス正規化・**既存「プロジェクト管理」への二重登録** |
| `pm_parser.js` | 111 | 【B系統】プレフィックスからintent判定(`pmClassifyIntent`)＋Gemini構造化抽出(`pmParseReport`) |
| `pm_pending.js` | 222 | 【B系統】金額/請求/入金の確認待ちワークフロー（`pmQueuePending`/`pmApprovePending`、経理通知） |
| `pm_router.js` | 285 | 【B系統】テキスト/postback振り分けオーケストレーション(`pmRouteText`/`pmRoutePostback`) |
| `pm_taskbridge.js` | 58 | 【B系統】次回アクション→既存「タスク管理」へ自動タスク作成（重複防止） |
| `pm_calendar.js` | 109 | 【B系統】`projects`日付項目→Googleカレンダー冪等同期 |
| `pm_drive.js` | 38 | 【B系統】案件Driveフォルダツリー自動作成（冪等） |
| `pm_alert.js` | 136 | 【B系統】放置/請求漏れ/入金未確認アラート（毎朝`pmCheckAlerts`） |
| `pm_organize.js` | 399 | 【B系統】シート整理・ダッシュボード生成・装飾（冪等・非破壊） |
| `pm_tests.js` | 293 | 【B系統】単体/E2E/診断(`pmDiagnose`) |
| `migrate.js` | 170 | データ移行補助（Driveオーナー移行・仮タスク一括転記） |
| `migrate_admin.js` | 317 | admin@woodbasef.com へのコピー型移行ヘルパー |

外部依存: LINE Channel API / Google Sheets / Drive / Calendar / Docs / Gemini 2.5 Flash。

### 1.2 Webhookエントリーポイントと分岐フロー

```
doPost(e)                              [コード.js:88]
  ├ verifyLineWebhook(e)               署名検証
  ├ isWebhookEventProcessed(ev)        冪等性チェック（重送排除）
  └ handleEvent(ev)                    [コード.js:156] イベント種別で分岐
      ├ postback → handlePostback(ev)  [コード.js:1038]
      │     └ ① pmRoutePostback(...)   [pm_router.js] ★最優先（pm_approve/pm_reject/pm_pick）
      │       ② 既存postback(set_project/new_project/ignore…)
      ├ follow/join/memberJoined → registerMember / handleBotJoinGroup
      └ message
          ├ ファイル/画像 → saveFileToDrive
          ├ linkGroupMode/newProjMode/retryMode → 各finalize
          └ text → isGroup ? handleGroup : handleDM
```

**DM分岐（`handleDM` コード.js:236〜、先勝ち `if(handler()) return;`）**
```
1. handleGoogleCalendarConnectRequest
2. handleEstimateSettingsListRequest   「見積設定一覧/見積一覧」
3. handleEstimateCommand               【パイオニア見積】★東邦より先
4. handleEstimateCreateRequest         【東邦見積】
5. handleProgressQuestionRequest       【A系統 進捗質問】★pmより先
6. pmRouteText                         【B系統 案件更新/新規案件/請求/入金】
7. isCompletionReport → handleCompletion
8〜 会話まとめ/ボールホルダー/メモ/住所会社/デフォルトQ&A
```

**グループ分岐（`handleGroup` コード.js:282〜）**
```
1. メンション判定（@WBG/＠WBG/秘書: 等のプレフィックス or LINE mention）
2. pmRouteText                         ★メンション不要で反応（プレフィックスのみ）
3. botMentioned のときだけ handleMentionCommand
     └ 見積→進捗質問(A系統)→完了報告→まとめ→… （handleDMとほぼ同列、ただしpmRouteTextは含まない）
```

### 1.3 意図判定の方式 ＝ **(b) キーワード/プレフィックス文字列マッチ**

- 機能分岐そのものは**ハードコードされた`if`の連鎖＋正規表現**で、AIによる意図判定ではない。
- B系統のintentは `PM_PREFIX_RULES`（pm_parser.js:10-18）の正規表現テーブル。
  ```
  案件更新|進捗更新|進捗報告 → project_update
  新規案件|案件登録         → project_create
  請求更新|請求報告 / 請求:  → billing_update
  入金更新|入金報告 / 入金:  → payment_update
  予定追加|カレンダー追加    → calendar_update
  ```
- **Geminiは「分岐後の中身の構造化抽出」にのみ使う**（`pmParseReport`/`extractWithGemini`/`callGemini`）。分岐判定には使っていない。
- **先勝ちの優先順位**が全体挙動を支配。例: 「見積設定一覧」は進捗質問より先、「パイオニア見積」は東邦見積より先、A系統(progress)はB系統(pm)より先。

### 1.4 スプレッドシート/シート構成

**メインスプシ（`SPREADSHEET_ID`）内**
| シート | 主な読み書き | 系統 |
|---|---|---|
| `projects` | pm_manager/pm_core | B系統（案件マスタ・正） |
| `project_logs` | pm_manager | B系統（更新履歴） |
| `project_alerts` | pm_alert | B系統（重複送信防止） |
| `project_calendar_events` | pm_calendar | B系統（カレンダー冪等） |
| `project_pending_updates` | pm_pending | B系統（承認待ち） |
| `PM設定` | pm_core | B系統（Script Property代替） |
| `ダッシュボード` | pm_organize | B系統（集計） |
| **`プロジェクト管理`** | コード.js(`registerNewProject`/`identifyProject`)＋pm_manager | **既存（案件名辞書＋グループ紐付け）** |
| `タスク管理` | コード.js/pm_taskbridge/summary | 既存＋B連携 |
| `スケジュール管理` | コード.js/pm_router | 既存＋B連携 |
| `メンバー管理` | コード.js/pm_router/pm_alert | 既存 |
| `メッセージログ` | summary | 既存 |
| `見積連携設定`/`見積単価設定`/`見積要確認` | estimate.js | 見積（東邦・設定） |
| `案件メモ`/`会社情報`/`仮タスク`/`完了タスク` | コード.js/migrate | 既存 |

**別スプシ（外部）**
| 用途 | ID取得元 | 参照 |
|---|---|---|
| **進捗管理表（A系統）** | `PROGRESS_SPREADSHEET_ID`(Script Prop) | progress.js |
| パイオニア 制作一覧 | `EST_PIONEER.SOURCE_SS_ID`（ハードコード） | estimate_pioneer.js |
| パイオニア テンプレ | `EST_PIONEER.TEMPLATE_SS_ID`（ハードコード） | estimate_pioneer.js |
| 東邦 工程管理表/テンプレ | 見積連携設定シートの各行 | estimate.js |

> **注目点**: 案件の進捗を表すデータが **`進捗管理表`（A系統・外部）/ `projects`（B系統）/ `プロジェクト管理`（既存・案件辞書）** の**3か所**に分散している。

---

## Step 2: 進捗管理機能の重複調査

### 2.1 2系統の正体

| | **A系統「個別の進捗管理表」** | **B系統「プロジェクト全体の進捗管理」** |
|---|---|---|
| 実装 | `progress.js`（688行・単独） | `pm_*.js`（11ファイル・2026-06-18追加） |
| 対象シート | 外部`進捗管理表`（`PROGRESS_SPREADSHEET_ID`） | メイン内`projects`ほか |
| データ構造 | **1店舗=1行・平面**。列=操作ステップ（施工月/図面チェック/発注/見積…）。状態は○△確認中等を**手動入力** | **1案件=1行＋`project_logs`蓄積**。5フェーズ×各ステータス＋金額/請求/入金 |
| フェーズ概念 | **なし**（「施工月」で時間軸を近似） | **あり**（5フェーズを`PM_PHASES`で定義） |
| トリガー | **自然文の質問**（`handleProgressQuestionRequest` progress.js:427）読取専用 | **明示プレフィックス報告**（`pmRouteText` pm_router.js:11）読み書き |
| 更新 | LINEからは**更新しない**（人がシート直接編集） | LINE報告→高信頼度は自動反映/金額等は承認制 |
| UI | テキスト箇条書き | テキスト＋Quick Reply（承認/却下/案件選択） |
| 役割 | 施工チェックリストの**照会** | 案件ライフサイクルの**管理** |

→ 設計書（`プロジェクト進捗管理_設計書.md` §1.1）も「progress.js＝外部進捗管理表への自然文クエリ、**今回の機能とは別物**」と明記。**根本的に別物**だが、ユーザー視点では両方「進捗」で、線引きが曖昧になっている。

### 2.2 重複・矛盾の具体的指摘

**① 案件データの三重管理（最重要）**
`pmEnsureProjectRecord`（pm_manager.js:40-69）が新規案件を **`projects`へ追記（46行）→ `registerNewProject`で「プロジェクト管理」へも追記（65行）** と二重登録。さらにA系統の`進捗管理表`は完全独立で同期なし。
- 結果: 同一案件が最大3シートに別形式で存在。`プロジェクト管理`にはフェーズ列がなくステータスは「進行中」固定（コード.js `registerNewProject`）、`projects`は5フェーズ×ステータス、`進捗管理表`はチェックリスト。**正がどれか不明**。

**② 5フェーズ定義の孤立**
`PM_PHASES`/`PM_PHASE_COLUMN`（pm_core.js:36-51）が**唯一の5フェーズ定義**。progress.jsは一切参照せず「施工月」列でフィルタ（progress.js `extractProgressItemsByQuestion`）。同じ「施工」が、B系統では7段ステータス、A系統では月＋チェック列、と**互換性ゼロ**。

**③ ルーティングの非対称（コンテキスト依存で挙動が割れる）**
- DM: `handleProgressQuestionRequest`（A・コード.js:244）→`pmRouteText`（B・246）の順。両方使える。
- グループ非メンション: `pmRouteText`（B・コード.js:298）のみ。**A系統は呼ばれない**＝「図面チェックできてない？」はグループで無反応。
- メンション: `handleMentionCommand`内に`handleProgressQuestionRequest`はあるが`pmRouteText`は**ない**（コード.js:388-392）。逆にB系統が使えない。
→ 同じ発話が「DM/グループ/メンション」で別モジュールに行き着く、または無反応。意図的な“グループ無反応”設計の副作用だが、A/B境界が混乱の温床。

**④ ステータス語彙の非統一**
A系統＝自由入力（○/△/確認中/未/保留…、progress.js`PROGRESS_DONE_TEXTS`）、B系統＝固定マスタ（`PM_PHASES`、不一致は承認待ち`pmProgressHoldReason`）、既存＝「進行中」固定。3種の語彙体系が併存。

**⑤ 「進捗更新」と「案件更新」の重複intent**
`PM_PREFIX_RULES`で`案件更新`も`進捗更新`も同じ`project_update`に落ちる（pm_parser.js）。ユーザーから見た使い分けがない＝プレフィックスの冗長。

---

## Step 3: 見積書機能の調査

### 3.1 発火トリガー（2系統・先勝ち）

| 系統 | 判定関数 | 条件 | 例 |
|---|---|---|---|
| パイオニア | `isPioneerEstimateCommand_`（estimate_pioneer.js:595） | `パイオニア\|什器` **かつ** `見積` **かつ** 作成動詞 | 「パイオニア 見積書作って」 |
| 東邦 | `isEstimateCreateRequest`（estimate.js:209） | `見積(書)?` ＋ `作って/作成/更新/上書き` 等 | 「東邦 5月分の見積作って」 |

- **コード.js:240でパイオニアを先に判定**→ True返却で東邦に到達しない（ショートサーキット）。
- 特殊分岐: パイオニアは「テスト/1店舗」を含むと`runEstimateTestOneStore`（先頭1店舗のみ）。
- いずれも**社名/対象がメッセージに明示されている前提**。「このプロジェクトの見積もりほしい」のような**案件IDベースの汎用発話では発火しない**（社名キーワード必須）。

### 3.2 データ参照元 ＝ **設定対応の東邦／ハードコードのパイオニア**

**東邦（estimate.js）＝半設定ベース**
- 連携設定: `見積連携設定`シート（工程スプシID・シート名・見積テンプレID・テンプレ名）を`getEstimateLinkSettings`（85行）で読む。
- 単価: `見積単価設定`シートを`getEstimateRateSettings`（182行）で読み、`getEstimateRate`（371行）で分類×キーワードから単価決定。
- **ただしセル座標は東邦テンプレ専用ハードコード**: `COVER_ITEM_START_ROW=14`/`DETAIL_START_ROW=53`/`TOTAL_ROW=10`/`TAX_RATE=0.10` 等（estimate.js:18-53）。デフォルト単価（修正5000/新規10000/カウンター15000/建具20000）も`setupEstimateForToho`（817-845）にハードコード。

**パイオニア（estimate_pioneer.js）＝ほぼ全ハードコード**
- スプシID・シート名・税率・単位・注記文言を`EST_PIONEER`オブジェクト（12-22）に直書き。
- ただしセル座標は持たず、テンプレのヘッダー行から`detectEstimateLayout_`（405-443）で**自動検出**（レイアウト変更に強い）。
- 設計コメント（4-8行）が「既存・estimate.jsと**完全独立**、グローバル衝突回避のため`EST_PIONEER`に閉じる」と明言＝**設定を外部から読む口がそもそも無い**。

### 3.3 出力先・フォーマット
- 東邦: 見積テンプレスプシ内に月別シートをコピー生成（`copyEstimateTemplateSheet`544 →`writeEstimateCover`/`writeEstimateDetails`/`writeEstimateTotals`）。会社情報/振込先/ロゴ/罫線は**テンプレ内に固定**、数値だけ転記。上書きは要明示。
- パイオニア: テンプレスプシ内に店舗別シート生成（`copyEstimateTemplate_`239 →`writeEstimateHeader_`/`writeEstimateDetails_`/`calculateEstimateTotals_`）。タイトルのプレースホルダ置換あり。同名はタイムスタンプ付与。

### 3.4 なぜ「設定すれば切り替わる」状態でないか

1. **2系統が共有コードゼロの完全別実装**。`copyEstimateTemplateSheet`(東邦) と `copyEstimateTemplate_`(パイオニア)、`writeEstimateTotals` と `calculateEstimateTotals_` が**同じことを別実装**。命名規則も別（`_`接尾辞有無）。新クライアント追加＝**3つ目のファイルを書く**流れになりかねない。
2. **テンプレ座標の扱いが両極端**。東邦＝座標ハードコード（別レイアウトのクライアントは定数総書き換え）、パイオニア＝自動検出だがID直書き。**「座標も設定として外部化」する共通レイヤーが無い**。
3. **クライアント/プロジェクトをキーに設定を引く構造が無い**。東邦は`見積連携設定`に複数行を持てるが「会社単位」で、案件IDから引く設計ではない。税率・項目・フォーマットを案件別に変える口は未整備。
4. 結果、クライアント別に変えたい場合の現実の作業は「estimate.js/estimate_pioneer.jsのどちらかをコピーし、ID・座標・単価・税率・文言を手で差し替える」になる。

---

## Step 4: 改善提案

> 原則: 既存の堅実な設計思想（非破壊setup・二段確認・誤名寄せ防止・名前空間分離）は**踏襲**。破壊的な作り直しはせず、**「正の一本化」と「設定外部化」**に絞る。

### 提案1: 進捗管理の統合

**考え方**: A系統（施工チェックリスト照会）とB系統（案件ライフサイクル管理）は**役割が違うので無理に1機能へ融合しない**。代わりに **`projects` を案件の「正」マスタに一本化**し、A系統は「`projects`配下の施工タスク・ビュー」として位置づけ直す。

- **データモデル**:
  - `projects`（B系統）を**唯一の案件マスタ**に確定。`プロジェクト管理`シートは「案件名↔グループID↔案件IDの索引ビュー」に降格（または`projects`から自動生成）。`identifyProject`/`getProjectNameByGroupId`の参照先を`projects`へ寄せ、**二重登録（pm_manager.js:65）を将来的に廃止**。
  - A系統の`進捗管理表`は当面**外部の実務シートとして存続**だが、各行に**案件ID列を1本追加**して`projects`と名寄せ可能にする（疎結合リンク）。「施工フェーズ配下のチェックリスト」という意味づけ。
- **関数構成**: フェーズ/ステータスの**単一の真実の源**を`PM_PHASES`（pm_core.js）に統一。progress.jsの完了判定もここを参照可能にする共通モジュール`pm_status`（仮）へ切り出し。
- **ルーティング統一**: 進捗系の入口を一本化する薄いディスパッチャを設け、**DM/グループ/メンションで同じ判定順序**にする（A系統の質問がグループ非メンションで無反応になる非対称を解消、または「グループ無反応」を明示ポリシーとして固定）。

優先度:
- **今すぐ**: ①ルーティング非対称の是正方針を決める（A系統をグループでも拾うか否か）。②`進捗管理表`に案件ID列を足し名寄せ可能化（非破壊・列追加のみ）。③ドキュメントに「A=施工チェックリスト照会／B=案件管理」の役割境界を明文化。
- **中期**: ④`プロジェクト管理`シートを`projects`から導出する形に変え二重登録を撤廃。⑤フェーズ/ステータス判定を共通モジュール化。

### 提案2: 見積もり設定の外部化

**考え方**: **「見積プロファイル」設定シート1枚**を新設し、クライアント/プロジェクトをキーに**ID・座標・単価・税率・項目・文言・出力先**を引く構造へ。コードは「プロファイルを読んで実行する1つのエンジン」に集約。

- **設定シート案 `見積プロファイル`**（1行=1クライアント/テンプレ種別）:
  | キー | 例 |
  |---|---|
  | profile_id / 表示名 | `pioneer` / パイオニア什器 |
  | トリガーキーワード | `パイオニア,什器` |
  | source_ss_id / source_sheet | 制作一覧スプシID / 制作一覧 |
  | template_ss_id / template_sheet | テンプレID / 原本 |
  | レイアウト方式 | `auto`(検出) / `fixed`(座標指定) |
  | 座標群（fixed時） | cover_start_row=14, detail_start_row=53… |
  | 税率 / 単位 / 注記文言 | 0.10 / 台 / ※記載無き… |
  | 単価参照 | `見積単価設定`の参照キー |
  | 出力先フォルダ | （任意） |
- **エンジン統合**: `estimate.js`/`estimate_pioneer.js`の共通処理（テンプレコピー・明細転記・合計計算）を**1モジュール`estimate_core`へ統合**し、差分は**プロファイルのパラメータ**で吸収。`detectEstimateLayout_`（パイオニアの自動検出）は`auto`方式として全プロファイルで再利用。
- **トリガー汎用化**: 社名キーワードに加え、**案件に紐づくprofile_id**を`projects`に持たせ、「（案件名）の見積もりほしい」→案件解決→profile決定、という案件ID起点の発火を可能に。

優先度:
- **今すぐ**: ①`見積プロファイル`シートのスキーマ確定（既存`見積連携設定`を拡張する形が自然）。②現状の東邦/パイオニアのハードコード値を棚卸ししてプロファイル1行ずつに転記（コードは据え置きで設定だけ先に作る）。
- **中期**: ③共通エンジン`estimate_core`への統合と2ファイルの段階移行（まず新規クライアントをエンジン側で、既存2社は動作維持→順次移植）。④案件ID起点トリガー。

### 提案3: 全体構成の整理

**考え方**: pm_*が示した「**接頭辞名前空間＋小さいファイル＋設計書**」は良い型。これを**estimate系・progress系にも横展開**し、`コード.js`モノリスを段階的に薄くする。

- **命名規則の統一**: 機能ごとに接頭辞（`pm_`/`est_`/`prog_`）。トップレベル関数は接頭辞必須（GASは全`.js`が同一グローバル名前空間のため衝突防止が必須）。
- **共通処理の切り出し**: `コード.js`に集中する`getSS`/`getSheet`/`callGemini`/`sendLineReply`/`identifyProject`等を**`core_*`（shared）モジュール**へ抽出。各機能はcoreに依存、機能間は疎結合。
- **ルーティングの明示化**: handleDM/handleGroup/handleMentionCommandに散る`if`連鎖を、**「ハンドラ登録テーブル＋優先度」**へ寄せると、機能追加時に「どこに何番目で差すか」が一目で分かり、先勝ち事故を防げる。
- **ドキュメント運用**: 機能追加時は「設計書追記→実装→pm_tests相当の単体/E2E」をテンプレ化（pm系で既に確立済みの流れ）。

優先度:
- **今すぐ**: ①新規機能追加のルール（接頭辞・1機能1ファイル群・設計書必須）を1ページに明文化。②`コード.js`の関数を「Webhook/ルーティング/共通/既存タスク」のセクションコメントで区切り、抽出候補を可視化。
- **中期**: ③共通処理の`core_*`抽出。④ルーティングのテーブル化。⑤estimate/progressの接頭辞リネーム。

---

## 合意が必要な論点（実装前に要確認）

1. **進捗の正をどうするか**: `projects`を唯一の正にし`プロジェクト管理`を索引へ降格／`進捗管理表`は施工チェックリストとして存続（案件ID列で名寄せ）――この方向で良いか。
2. **A系統の運用継続**: `進捗管理表`は当面継続か、将来`projects`へ統合・廃止か。継続なら名寄せ列の追加可否。
3. **グループでの進捗質問**: グループ非メンションで「図面チェックできてない？」に答えるべきか（現状無反応）。それとも「グループは無反応・メンション時のみ」を正式ポリシーにするか。
4. **見積の単位**: 設定キーを「クライアント単位」とするか「案件（プロジェクトID）単位」まで細かくするか。
5. **移行の同時運用**: 見積エンジン統合中、東邦/パイオニアの既存2社は現行コードのまま温存し新規のみ新エンジン、という二重稼働を許容するか。

---

## 確定方針（2026-06-30 ユーザー回答）

> **基本方針**: 既存機能を壊さず、入り乱れた機能を**整理・統合**する。今後の機能追加は**「大きな項目（ドメイン）の中で拡張するだけ」**で済む形に寄せる。現状かぶっている機能はまとめる。
>
> **見積の粒度**: **両対応**（案件＝プロジェクトIDの設定があれば優先、無ければクライアント単位にフォールバック）。

### A. 「大きな項目（ドメイン）」への再編 ＝ 拡張のしやすさを担保

機能を以下の**安定したドメイン**に整理し、今後の追加は必ずどれかの配下に入れる（新トップレベル機能を増やさない）。各ドメインは接頭辞で名前空間を分離。

| ドメイン（大項目） | 接頭辞 | 含む現行機能 | 今後の拡張はここに足すだけ |
|---|---|---|---|
| 入口・ルーティング | `route_` | doPost/handleEvent/handleDM/handleGroup/handlePostback | 新コマンドはハンドラ登録テーブルに1行追加 |
| 案件・進捗 | `pm_` | **A系統 progress＋B系統 pm を統合**。projectsが正、進捗管理表は施工チェックリストビュー | 新フェーズ項目・新ステータスは`PM_PHASES`に追記 |
| タスク・予定 | `task_` | writeTask/カレンダー/スケジュール | 新タスク種別を足すだけ |
| 見積 | `est_` | 東邦＋パイオニアを**1エンジン＋プロファイル**へ統合 | 新クライアントは`見積プロファイル`に1行追加 |
| アラート・通知 | `alert_` | pm_alert/毎朝ダイジェスト | 新アラート条件を1関数追加 |
| サマリー | `sum_` | summary.js | 出力先・テンプレ追加 |
| 共通基盤 | `core_` | getSS/getSheet/callGemini/sendLineReply/identifyProject | 共有処理の置き場 |

→ 「機能追加のたびに同じ混乱が起きる」のを防ぐ仕組み。**新規＝既存ドメインの設定行/小関数追加**で完結させる。

### B. 進捗管理の統合（破壊しない・かぶりをまとめる）

> **改訂(2026-06-30)**: 実コード精査の結果、`プロジェクト管理`は索引でなく**案件解決の心臓部**（identifyProject等が全面依存）と判明。「降格・撤廃」は回帰リスク大のため取り下げ、**「案件IDで二面一体（両シートを残し同一IDで結ぶ）」**へ改訂。詳細は `フェーズ1_整理プラン_案件ID統合.md`。以下の元記述は経緯として残す。

- ~~**`projects` を案件の唯一の正**に確定。`プロジェクト管理`シートは**`projects`から導出する索引ビュー**へ降格（既存の`identifyProject`/`getProjectNameByGroupId`は参照先を`projects`に寄せる）。**二重登録（pm_manager.js:65）は段階的に撤廃**。~~ → 改訂: 両シート維持・`案件ID`列で1対1リンク・単一作成経路で同一IDを刻む。
- **A系統`進捗管理表`は存続**（実務で使うチェックリスト）。ただし**案件ID列を1本追加**して`projects`と名寄せ＝「施工フェーズ配下の施工チェックリストビュー」と意味づけ。融合せず役割で分担＝かぶりの解消。
- **フェーズ/ステータスの真実の源を`PM_PHASES`に一本化**。progress.jsの完了判定も同じ語彙を参照（`core_status`へ切り出し）。
- **ルーティングを統一**（A系統がグループ非メンションで無反応／メンションでB系統が呼ばれない非対称を解消）。「進捗系の入口」を1つのディスパッチャに集約。
- いずれも**列追加・参照先変更のみで非破壊**。既存データ・既存LINE挙動は維持。

### C. 見積の統合（両対応フォールバック）

- **`見積プロファイル`シート1枚**を新設（既存`見積連携設定`を拡張）。プロファイル解決は **案件ID → 無ければクライアント/トリガーキーワード** の順でフォールバック。
- `projects`に`見積プロファイルID`列を追加 → 「（案件名）の見積もりほしい」で案件解決→プロファイル決定が可能に（社名キーワード必須の制約を緩和）。
- 共通処理（テンプレコピー/明細転記/合計計算/レイアウト検出）を**`est_core`に統合**。東邦＝`fixed`座標、パイオニア＝`auto`検出を**プロファイルのパラメータ**で吸収。**既存2社は現行動作を維持したまま、まず新規クライアントを新エンジンで**（二重稼働を許容しつつ順次移植）。

### 実装の進め方（合意済み方針に沿った優先順）

**フェーズ0（今すぐ・非破壊の足場）**
1. ドメイン境界と命名規則を1ページに明文化（本書のA章を運用ルール化）。
2. `進捗管理表`に案件ID列を追加（列追加のみ）。`projects`に`見積プロファイルID`列を追加。
3. `見積プロファイル`シートのスキーマ確定＋東邦/パイオニアの現ハードコード値を1行ずつ転記（コードは据え置き）。

**フェーズ1（中期・統合本体）**
4. フェーズ/ステータス・共通送信・案件解決を`core_*`へ切り出し、進捗ルーティングを一本化。
5. `プロジェクト管理`を`projects`導出の索引へ移行し二重登録撤廃。
6. `est_core`エンジン化、新規クライアントから移行。

---

※実装はフェーズ0から着手予定。各フェーズ着手前に対象ファイルと差分方針を提示し、確認を取ってから`clasp push`（編集アカウント woodbasegroup@gmail.com）で反映する。
